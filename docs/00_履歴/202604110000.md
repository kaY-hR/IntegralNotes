# 2026-04-11 00:00

## 背景

- `scripts/export-tracked-files.ps1` に日本語を含む `TargetRoot` を直接書いたところ、共有先フォルダ名が文字化けする懸念が出た。
- 実際に Windows PowerShell 5 系では、BOM なし UTF-8 の `ps1` 内にある日本語リテラルが ANSI 扱いされ、`共有` が `蜈ｱ譛噂` のように崩れる再現が取れた。

## 実施内容

- `scripts/export-tracked-files.ps1`
  - `TargetRoot` の直上に「非 ASCII を含むなら UTF-8 with BOM を維持すること」をコメントで追記した。
  - ファイル自体を UTF-8 with BOM に再エンコードした。

## 検証

- `scripts/export-tracked-files.ps1`
  - 先頭バイトが `EF BB BF` であることを確認
- 再現テスト
  - BOM 付きの最小 `ps1` で `共有\test-bom` を作成し、実際のフォルダ名が文字化けせず `共有\test-bom` になることを確認
- 実共有先確認
  - `C:\Users\shimadzu\OneDrive - SHIMADZU\共有` を `dir /x` で確認し、少なくとも現在見えている配下には `test` が存在し、文字化けした別名フォルダは確認できなかった
