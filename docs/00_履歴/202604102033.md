# 2026-04-10 20:33

## 背景

- `docs/00_履歴/ファイル追加.png` と `docs/00_履歴/フォルダアイコン15.png` を使っているなら、`docs` 直参照ではなく `src` 配下へコピーして使うよう整理したい、という指摘があった。

## 実施内容

- `src/renderer/App.tsx`
  - Explorer ヘッダーの追加アイコン参照を `../../docs/00_履歴/...` から `./resources/...` へ変更した。
- `src/renderer/resources/`
  - `ファイル追加.png`
  - `フォルダアイコン15.png`
  - 上記 2 ファイルを追加し、renderer のソース管理配下へ移した。

## 検証

- `npm run build`
  - 成功
  - Vite build 出力に `dist/assets/ファイル追加-*.png` と `dist/assets/フォルダアイコン15-*.png` が含まれることを確認
