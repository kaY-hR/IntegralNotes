# 2026-04-14 12:54

## 会話要約

- Issue 13 の追加方針として、元データの能動登録は `cwd` 外も正式に扱い、`Data/` に visible alias を置く流れに整理した。
- あわせて、後方互換は不要という前提に合わせ、残っていた legacy migration / legacy block type / legacy plugin manifest 正規化を削除した。

## 実施したこと

- `docs/Issues.md` の Issue 13 を更新し、以下を明記した。
  - `cwd` 外登録時は `.store` へ canonical copy、`Data/` に alias
  - `cwd` 内登録時は元 path を alias のまま維持
  - 後方互換コードは持たない
- `docs/10_要求`, `docs/20_アーキテクチャ`, `docs/30_設計` の関連文書を同方針へ更新した。
- `src/main/integralWorkspaceService.ts` を更新し、original data 登録処理を次のように変更した。
  - `cwd` 外: `.store` へ copy して `Data/` に alias を作成
  - `cwd` 内: `.store` へ移して元 path に alias を再配置
  - metadata / summary / data note に `aliasRelativePath` と `Alias Path` を追加
  - `Notes`, `data-catalog`, `Data`, `.store`, `.py-scripts` 配下の再登録を禁止
- `src/main/workspaceService.ts` を簡素化し、legacy migration 一式を削除して、現行 `.store/.integral` からの data note 同期のみに整理した。
- `src/main/pluginRegistry.ts` から旧 Shimadzu plugin schema の救済処理を削除した。
- `src/renderer/integralBlockRegistry.tsx` から旧 display block type (`chunk-view`) の互換処理を削除した。
- `src/renderer/DataRegistrationDialog.tsx` の説明文とボタン文言を新導線に合わせて更新した。

## 確認結果

- `npm run build` は成功した。
- `src` 配下を検索し、legacy 互換コードとして残していた `links.json`, `.original-data`, `chunk-view`, `shimadzu.lc`, `LC.Method.Gradient` 参照は除去されていることを確認した。
