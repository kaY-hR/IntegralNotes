# 2026-04-14 10:15

## 会話要約

- 用語整理の方針として、
  - `Artifacts` -> `data-catalog`
  - `artifact note` -> `data-note`
  - `blob` -> `original data`
  - `chunk` -> `dataset`
  を採る方向で合意した。
- この方針で大規模 rename を進める前に、まずは `$add-issue` として `docs/Issues.md` に追加する依頼があった。

## 実施内容

- `docs/Issues.md`
  - `## [ ] 9. 用語を data-catalog / data-note / original data / dataset ベースに大規模リネームしたい`
    を追加した。
  - issue には、
    - rename 対象の主要用語
    - 実施順 `1. 文書 2. コード 3. フォルダ命名規則 4. UI`
    - `source chunk / derived chunk / source-bundle` 周辺用語の再整理
    - workspace path / note / link への影響と migration 検討
    を記録した。
