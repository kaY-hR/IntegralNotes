# 2026-04-19 06:34

## 概要

- 「リリース前で後方互換を無視して理想設計へ寄せる」方針に合わせ、data-note への output 投影契約を `share_note_with_input + embed_to_shared_note` に一本化した。
- 旧 `project_to_inputs` ベースの追記先指定は src 実装から除去した。

## 実装

- `src/shared/integral.ts`
  - `IntegralSlotDefinition` から `projectToInputs?: string[]` を削除。
- `src/main/integralWorkspaceService.ts`
  - `resolveProjectedInputSlotNames()` を「`embedToSharedNote` が true のときだけ `shareNoteWithInput` を投影先として返す」実装へ変更。
  - 旧 `projectToInputs` へのフォールバックを削除。
  - Python slot parser から `project_to_inputs` の解釈を削除し、output slot には `embed_to_shared_note` / `share_note_with_input` のみ反映するように変更。

## 確認

- `npm run build` 成功
