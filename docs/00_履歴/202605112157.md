# 2026-05-11 21:57 shimadzu-lc install と block picker 表示修正

## 背景

`plugins/shimadzu-lc` で `npm run build` を実行すると、plugin 側に `build` script が無いため失敗した。

`shimadzu-lc` は standalone runtime plugin として `%LocalAppData%/IntegralNotes/runtime-plugins/shimadzu-lc` に install する構成だった。

## 実施

- `plugins/shimadzu-lc` で `npm run install:local` を実行し、`C:\Users\shimadzu\AppData\Local\IntegralNotes\runtime-plugins\shimadzu-lc` へ install した。
- build 済み main process の `PluginRegistry` から `shimadzu-lc` が installed plugin として読めることを確認した。
- `src/renderer/MilkdownEditor.tsx` を修正した。
  - `>` picker は Python callable 専用のまま維持した。
  - Crepe の `/` slash menu に `Runtime Blocks` group を追加し、runtime plugin block だけを挿入できるようにした。
  - `shimadzu-lc/run-sequence` は `/` 側から `Run Sequence (Shimadzu LC)` として挿入する方針にした。

## 確認

- `npm run build` 成功。
