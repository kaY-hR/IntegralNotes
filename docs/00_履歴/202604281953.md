# 2026-04-28 会話まとめ

## 目的

Python block 実行時のエラー表示仕様を要求文書に明記する。

## 実装内容

- `docs/10_要求/10_MVP要件.md` の「10. Python 実行」にエラー表示仕様を追記した。
- exit code が非 0 の場合は Python の `stderr` / `stdout` / runner error message を note 上で優先表示することを明記した。
- exit code が 0 でも宣言された output path が作成されていなければ、app 側の実行エラーとして note 上に表示することを明記した。

## 変更ファイル

- `docs/10_要求/10_MVP要件.md`
- `docs/00_履歴/202604281953.md`

## 確認

- 文書のみの変更のため、ビルド・テストは未実行。
