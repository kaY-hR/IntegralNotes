# 2026-04-14 20:30

## 会話要約

- `Issue 20` として、frontmatter 分離を `data-catalog` 配下の data-note 限定ではなく、`cwd` 配下の Markdown 全体の標準動作に広げたい依頼に対応した。
- 途中で、実装だけでなく要求・設計文書の更新も合わせて行うよう依頼を受けた。

## 実施したこと

- `docs/00_履歴` の直近 3 件と `docs/10_要求`、`docs/Issues.md` を確認し、frontmatter 隠蔽の既存方針と `Issue 20` の要求を整理した。
- `src/main/frontmatter.ts` を追加した。
  - `---` / `...` fence を使う generic frontmatter utility を用意した。
  - frontmatter 本体は保持しつつ、editor / viewer に渡す本文だけを抽出できるようにした。
- `src/main/workspaceService.ts` を修正した。
  - `.md` の read / save を `data-catalog` 限定から外し、`cwd` 配下 Markdown 全体で frontmatter と本文を分離するようにした。
  - 保存時は本文だけを更新し、既存 frontmatter は保持するようにした。
  - rename / move 時の Markdown link / image target rewrite は本文だけに適用し、frontmatter 内の任意 metadata は保持するようにした。
- `src/main/dataNote.ts` を修正した。
  - data-note 専用の frontmatter 処理も新しい utility を使うように寄せ、managed metadata と一般 frontmatter が同じ基盤で動くようにした。
- 文書を更新した。
  - `docs/10_要求/10_MVP要件.md`
  - `docs/20_アーキテクチャ/10_データモデル.md`
  - `docs/30_設計/20_ワークスペースレイアウト.md`
  - `docs/Issues.md` の `Issue 20` を completed に更新し、実装内容を追記した。

## 検証

- `npm run build` を実行し、renderer / main ともに build が通ることを確認した。

## 補足

- 今回の整理により、`data-note` は「特殊な frontmatter を持つ Markdown」の一種として扱われ、通常 note の任意 frontmatter と同じ read / save 基盤を共有する構成になった。
- raw markdown として frontmatter 自体を直接編集する UI は今回の対象外で、引き続き `Issue 3` 側の設計範囲になる。
