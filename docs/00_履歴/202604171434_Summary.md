# 2026-04-17 14:34 Summary

## 会話要約

- embed preview について、scroll 自体はできるようになったが、scrollbar が見えないという追加報告があった。
- そのため、interaction は維持したまま、scrollbar をより明示的に表示する方向で追加調整を行った。

## 実装

- `src/renderer/workspaceEmbedFeature.tsx`
  - iframe embed に `scrolling="yes"` を付けた。
  - text preview 用 `srcDoc` に `overflow-y: scroll` を明示し、scrollbar が出やすい HTML にした。
- `src/renderer/ExternalPluginFileViewer.tsx`
  - plugin viewer iframe にも `scrolling="yes"` を付けた。
- `src/renderer/styles.css`
  - readonly markdown preview の scroll container を `overflow-y: scroll` に変更し、`scrollbar-gutter: stable` を追加した。
  - embed surface を `overflow: visible` / `min-height: 0` に調整した。
  - embed 内の markdown / plugin / dataset preview に対して、scroll container が縮みつつ scrollbar を表示しやすい style に寄せた。

## 確認

- `npm run build` : 通過

## 補足

- GUI 実機での目視確認は未実施。
- もしまだ scrollbar が見えない場合は、`markdown だけ`, `html iframe だけ`, `plugin viewer だけ` のように種類を切り分けると次の修正が早い。
