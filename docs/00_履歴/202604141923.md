# 2026-04-14 19:23

## 会話要約

- 画像添付の文書化と Issue 化が一段落したため、本題の `workspace file` Markdown link 実装へ戻った。
- `Milkdown` 上で `[` から補完し、選択時に `[label](/path/from/cwd)` を挿入し、click で既存 open 導線へ繋ぐ実装を入れた。

## 実施したこと

- `src/shared/workspaceLinks.ts` を追加した。
  - workspace root 相対 target の解決
  - canonical target 生成
  - Markdown link / image target の rewrite
  を共通化した。
- `src/renderer/MilkdownEditor.tsx` を更新した。
  - `[` 入力中の query を selection から検出し、workspace file 候補 popup を表示するようにした
  - 候補選択時に `.md` は拡張子なし、それ以外は拡張子あり label で Markdown link を挿入するようにした
  - note 本文上の workspace link click を拾い、既存 file open 導線へ渡すようにした
  - editor 非制御構成を維持したまま、外部からの markdown 変更だけを `replaceAll` で反映するようにした
- `src/main/workspaceService.ts` を更新した。
  - rename / move 完了後に `cwd` 配下 `.md` を走査し、root-relative Markdown link / image target を自動更新するようにした
- `src/renderer/App.tsx` を更新した。
  - rename / move の path change を open markdown tab の `content / savedContent` にも適用するようにした
  - link open 時は unsupported file を既定アプリへ流せるようにした
- `src/renderer/styles.css` を更新した。
  - link 補完 popup の見た目を追加した
- `docs/Issues.md` の `Issue 22` を `completed` に更新した。

## 検証

- `npm run build` を実行し、renderer / main ともに build が通ることを確認した。

## 補足

- runtime 上での手動操作確認まではまだ行っていないため、補完 popup の体感、click open の細部、rename 時の実挙動は次に app 上で確認する余地がある。
- `Issue 23` の画像永続化はまだ未着手で、今回の実装対象には含めていない。
