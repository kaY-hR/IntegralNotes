# 2026-04-17 14:24

- エクスプローラーパネルの未動作項目を実装した。
  - `shift` による範囲選択を追加した。`ctrl/cmd + shift` では既存選択へ範囲追加できる。
  - 外部からのファイル・フォルダ drag and drop 受け入れ判定を強化した。`DataTransfer.types/items/files` を併用してディレクトリ drop も通るようにした。
  - `ctrl+v` / 右クリック `貼り付け` で、外部アプリがクリップボードへ置いたファイル・フォルダを取り込めるようにした。
- 内部の explorer copy/paste についても、renderer state 依存ではなく Electron clipboard の custom format を使うようにして、貼り付け元判定を安定化した。
- `src/main/main.ts` に Windows 向けの clipboard file-drop list 読み出しを追加し、`src/main/preload.ts` / `src/shared/workspace.ts` 経由で renderer から利用できるようにした。
- `src/renderer/App.tsx` では可視ツリー順の flatten を使って範囲選択を計算するようにした。
- 併せて `FileTree` 呼び出しへ `onDoubleActivateEntry` を渡し忘れていた箇所も補正した。
- `npm run build` を実行して通過を確認した。
  - 最初の sandbox 内実行は `esbuild` の `spawn EPERM` で失敗したため、sandbox 外で再実行した。
