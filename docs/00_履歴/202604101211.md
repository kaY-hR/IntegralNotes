# 2026-04-10 12:11

## 背景

- `shimadzu-lc` plugin の UI を `docs/10_要求/機能詳細イメージ/スライド4.PNG` に寄せたいという要望があった。
- 追加で、既存の `gradient.png` をそのまま使えるので、simple mode をスライド4の文書表示に近づけたいという意図が共有された。

## 実施内容

- `plugins/shimadzu-lc/renderer/index.html`
  - default view を `simple` に変更し、報告書風の文書レイアウトを追加した。
  - title / 目的 / 前回結果 / グラジエント条件 / 結果確認 / suggest card という流れで、スライド4に近い構成へ変更した。
  - `simple` mode は `gradient.png` をそのまま中央表示する軽い preview に整理した。
  - `detail` mode は従来どおり編集 UI を残しつつ、切替ロジックを `simple` default に合わせて修正した。
  - action button は suggest card 風の見た目に変更した。
- `plugins/dist/shimadzu.lc/`
  - 更新後の renderer を含む zip を再生成した。

## 検証

- `plugins/shimadzu-lc/renderer/index.html`
  - `<script>` 部分を `new Function(...)` でパース確認し、syntax error がないことを確認
- `plugins/dist/shimadzu.lc/shimadzu.lc-0.1.0.zip`
  - zip 更新時刻が `2026-04-10 12:11:05` になっていることを確認
