# 2026-05-03 17:49 py block skill / prompt の idts input 指示追加

## 依頼

- `.idts` input を受ける Python block 生成時に、`extensions: [".idts"]` を明示する必要があることを skill / prompt に記載すべきか確認した。

## 対応

- 指示不足と判断した。
- `.idts` は datatype ではなく bundle 表現であり、input picker の候補表示にも使われるため、`.idts` dataset を受ける input slot では `datatype` だけでなく `extensions: [".idts"]` も必須、というルールを追加した。

## 変更したファイル

- `.codex/skills/implement-integral-block/SKILL.md`
- `src/shared/aiChat.ts`
- `src/main/aiChatService.ts`

## 確認

- `npm run build` 成功。

## 備考

- 既存の外部 workspace script `C:\Users\shimadzu\Documents\Notes\scripts\lc_chromatogram_plot.py` と `C:\Users\shimadzu\Documents\Notes\scripts\visualize_chromatograms.py` は今回未変更。
