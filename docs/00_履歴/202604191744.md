# 2026-04-19 17:44

## 概要

- ユーザーが外出先の Codex Web で実装して commit した内容をローカルで確認した
- 確認対象は履歴 (`docs/00_履歴`) と最新 commit、主要な実装差分、build 結果

## 確認した commit

- `c15744d66622bd84ecacdd8bebe5983f019cf41f`
- message: `ノートを共有する仕組みを導入`

## 履歴

- 直近の履歴は以下を確認した
  - `docs/00_履歴/202604191734.md`
  - `docs/00_履歴/202604191300.md`
  - `docs/00_履歴/202604190948.md`
- `202604191734.md` には、`backup/main` 側の差分を patch apply で取り込んだ経緯が記録されていた
- `202604191300.md` には、`note owner / note target` 方向の概念整理と未決論点がまとまっていた

## 実装確認

- `src/shared/integral.ts`
  - output slot 仕様が `project_to_inputs` から `shareNoteWithInput` / `embedToSharedNote` に変更されていた
  - `IntegralManagedFileSummary` / `IntegralDatasetSummary` に `noteTargetId` が追加されていた
- `src/main/dataNote.ts`
  - data-note frontmatter に `noteTargetId` が追加されていた
- `src/main/integralWorkspaceService.ts`
  - output artifact 生成時に `noteTargetId` を持たせる処理が追加されていた
  - shared note 前提で projection 先を決める処理に変更されていた
- `src/main/workspaceService.ts`
  - `noteTargetId !== self` の artifact では専用 note を生成しない処理が追加されていた
- `src/renderer/App.tsx`
  - note open 時に `noteTargetId` を優先する変更が入っていた
- `docs/10_要求/*`, `docs/30_設計/*`, `docs/Issues.md`
  - 仕様文書も `share_note_with_input` / `embed_to_shared_note` ベースへ更新されていた

## 確認結果

- `git status --short` は空で、作業ツリーは clean
- `npm run build` は成功

## 気になった点

- `src/main/integralWorkspaceService.ts:1198` の `resolveOutputSlotSharedNoteTargetId()` は、input が通常 file の場合に `noteTargetId` として file の ID ではなく workspace path を返してしまう形になっている
- 実データでも `Notes/.store/.integral/DTS-43787C92.json` と `Notes/Data/PCA-Result.idts` の `noteTargetId` が `"/Data/LC.json"` になっていた
- `App.tsx` の `findManagedDataTargetById()` / `findManagedDataTargetForPath()` は dataset を見ておらず、dataset 側の `noteTargetId` 解決が UI 全体で一貫していない可能性がある

