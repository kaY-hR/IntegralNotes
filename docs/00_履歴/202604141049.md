# 2026-04-14 10:49

## 会話要約

- Python script 実行 block の UI について、不要な情報が多く一覧性が悪いという指摘があった。
- 特に、slot ごとの dataset 割り当てと `block-type` だけを主に見たい、という要求が整理された。

## 実施したこと

- `docs/Issues.md` に Issue 11「Python script 実行 block の UI を簡素化したい」を追加した。
- Issue には以下の改善要望を記録した。
  - `plugin-name` 枠の削除
  - `input` / `output` 数表示の削除
  - slot 名と設定済み dataset 名を `slot名: 設定dataset名` でまとめて表示
  - 余白を削って、よりフラットで情報密度の高い UI に寄せる
