# 2026-04-17 00:21

- Activity Bar に built-in `Search` view を追加した。
  - `builtin:explorer`
  - `builtin:search`
  - `builtin:plugins`
  の順で sidebar view を切り替えられるようにした。
- `src/renderer/SearchSidebarView.tsx` を追加し、workspace 全文検索 pane を実装した。
  - `query`
  - `replace`
  - `include files`
  - `exclude files`
  - `case sensitive`
  - `whole word`
  - `regex`
  を扱えるようにした。
- `replace` と file filter は foldable section とし、通常時は閉じた状態から使えるようにした。
- `src/shared/workspace.ts` に workspace search / replace 用の request / result 型を追加した。
- `src/main/main.ts`, `src/main/preload.ts` に `workspace:searchText` / `workspace:replaceText` IPC を追加した。
- `src/main/workspaceService.ts` に workspace text search / replace を実装した。
  - workspace を再帰走査して text file を収集する
  - `include files` / `exclude files` は簡易 glob として解釈する
  - image / binary は検索対象から外す
  - 検索結果は file ごとに `lineNumber / startColumn / endColumn / lineText` を返す
  - replace は同じ matcher で file を書き戻す
- renderer 側では dirty な markdown tab と衝突する file がある場合、replace を止めて先に保存を促すようにした。
- replace 後は workspace snapshot と open tab を再同期し、同じ条件で検索結果を再実行するようにした。
- 仕様文書を更新した。
  - `docs/Issues.md`
  - `docs/10_要求/20_ユーザー体験.md`
  - `docs/30_設計/80_ActivityBarとSidebarView.md`
- `npm run build` が通ることを確認した。
