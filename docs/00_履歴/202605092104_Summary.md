# 2026-05-09 21:04 履歴

## 背景

- `C:\Users\shimadzu\Desktop\_Root\00_Obsidian\Projects\01_Integral\動画A_白井先生テーマ模擬\Notes\20260508-スクリプト作成.md` の `detect_cells.py` block 実行時エラーを確認した。
- 実行ログ `Notes/analysis-result/integral-block-logs/BLK-A813AA73.log` では `ModuleNotFoundError: No module named 'integral_image_compare'` が出ていた。
- 原因は `detect_cells.py` が `skills/image-analysis-tips/sdk/integral_image_compare.py` に依存していたこと。
- package Python block import は `integral-package.json` と `scripts/` subtree だけを workspace `.packages/<packageId>/` に copy する設計のため、`skills/` 配下 SDK は実行時 import 対象にならない。

## 対応

- `integral_image_compare.py` を `plugins/image-analysis-pack/scripts/integral_image_compare.py` に追加した。
- `%LocalAppData%/IntegralNotes/packages/image-analysis-pack` を `npm run plugins:install:all` で再インストールした。
- 既に import 済みの対象 workspace にも `Notes/.packages/image-analysis-pack/scripts/integral_image_compare.py` を追加した。
- 仕様文書に、Python block が import する helper module は `scripts/` subtree に置き、`skills/` 配下 SDK / helper へ直接依存しないことを明記した。

## 確認

- 対象 workspace の `detect_cells.py` は import できるようになった。
- 対象画像を使って direct に `main()` を実行し、`Detection complete: total=62, fluorescence_positive=40, fluorescence_negative=22` まで成功することを確認した。
- 試験出力先は gitignore 済みの `dist/integral-detect-check/`。
