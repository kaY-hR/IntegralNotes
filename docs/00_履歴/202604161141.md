# 2026-04-16 11:41

## 会話概要

- ユーザー要望:
  - エクスプローラーパネルのメニューに目のアイコンを追加し、hidden フォルダの表示/非表示を切り替えたい
- 方針:
  - workspace snapshot 自体は hidden entry を保持し、renderer 側で tree 表示だけ切り替える
  - これにより、ファイル操作後も hidden 表示状態を崩さずに保てるようにする

## 実装内容

- `src/main/workspaceService.ts`
  - workspace tree 取得時に dot 始まり entry を除外しないよう変更
- `src/renderer/App.tsx`
  - hidden 判定を「relative path のどこかの segment が `.` 始まりか」で判定するよう修正
  - explorer 向けの visible entry filter を追加
  - `showHiddenEntries` state と toggle handler を追加
  - サイドバー header に目アイコン button を追加
  - tree、選択状態、初回 auto-open、Milkdown の workspace 候補に filtered entries を適用
- `src/renderer/styles.css`
  - 目アイコン SVG 用 style を追加
  - hidden 表示中の active button style を追加

## 検証

- `npm run build` 成功
