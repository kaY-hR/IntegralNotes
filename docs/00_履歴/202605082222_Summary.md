# 2026-05-08 22:22 画像比較 manifest 生成 skill の追加

## 依頼

- `.icv` と `.idts` の仕様統一は一旦見送る。
- `Notes/.codex/skills` の画像用 skill に、`.icv` file を作る helper script や、`.icv` を出力する Python block 実装用の SDK / 素材を配置する。

## 対応

- `Notes/.codex/skills/image-analysis-tips/SKILL.md` を更新した。
  - `.icv` output 作成時に読む reference と helper assets を明記。
  - `implement-integral-block` skill と併用する方針を追記。
  - Windows の `cv2.imread()` 日本語 path 問題に対する helper wrapper の案内を追記。
- `.icv` manifest 仕様 reference を追加した。
  - `Notes/.codex/skills/image-analysis-tips/references/image-compare-manifest.md`
- Python block から使える helper SDK を追加した。
  - `Notes/.codex/skills/image-analysis-tips/sdk/integral_image_compare.py`
  - `.icv` manifest 作成、layer 定義、入出力 path validation、OpenCV の Unicode path 対応 wrapper を含む。
- CLI helper を追加した。
  - `Notes/.codex/skills/image-analysis-tips/scripts/create_image_compare_manifest.py`
  - 画像 path 群から `.icv` manifest を作成できる。
- Python block template を追加した。
  - `Notes/.codex/skills/image-analysis-tips/templates/image_compare_block.py`
  - 2 枚の位置合わせ済み画像から `.icv` を出力する `@integral_block` の雛形。

## 確認

- 追加した Python helper / script / template を `python -m py_compile` で構文確認。
- `image_compare_block.py` を `validate_integral_block.py` で検証し、`OK: 1 block(s) checked, 0 warning(s).` を確認。
- CLI helper の `--help` 表示を確認。

## 注意

- `.icv` は現時点では path-based manifest とし、managed data ID / `.idts` metadata 連携は見送った。
- `.icv` output は通常の single-file output slot として扱う。`.idts` output helper は使わない。
