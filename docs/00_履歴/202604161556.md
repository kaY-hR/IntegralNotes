# 2026-04-16 15:56

- 仕様文書のうち、直近実装とズレていた Python block 挿入導線、`itg-notes` YAML 専用化、`.idts` embed の例外仕様を更新した。
- `docs/10_要求/10_MVP要件.md` では `itg-notes` source を YAML 専用と明記し、Python callable discovery を slash menu ではなく `>` popup 前提へ修正した。runner が `analysis-args.json` を読み、callable を `inputs / outputs / params` 引数で呼ぶことも追記した。
- `docs/10_要求/20_ユーザー体験.md` では、Python block の挿入フローを `>` popup 起点へ更新した。
- `docs/20_アーキテクチャ/30_Python汎用解析プラグイン.md` と `docs/30_設計/30_Pythonスクリプト登録と実行.md` では、現行 scan 契約が `@integral_block(...)` の直後に `def ...(` が続く regex 検出であること、decorator import は現状 app runner が `sys.modules["integral"]` に差し込む no-op shim で成立していること、将来的には別管理の Python SDK へ切り出したい方針を明記した。
- `docs/30_設計/10_ブロックJSON設計.md` には user-facing source が YAML 専用であり JSON 互換を持たないことを追記した。
- `docs/30_設計/40_標準描画と結果閲覧.md` では、`.idts` embed だけは一般 viewer plugin 解決とは別に既存 DataSetViewer 系 preview へ流す例外仕様を明記した。
- デモ用 Python callable として `scripts/demo_dataset_report.py` を追加した。`@integral_block(...)` を付けた `main` 関数が入力 dataset を集計し、output dataset に `index.html`, `summary.json`, `files.csv`, `README.txt` を生成する。
- `scripts/demo_dataset_report.py` は AST parse で文法チェック済み。
