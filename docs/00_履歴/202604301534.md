# 2026-04-30 15:34 main 取り込み

## 目的

`non-prompt-ai-insert` branch に `origin/main` の最新変更を取り込む。

## 対応内容

- `origin/main` を fetch し、`non-prompt-ai-insert` へ merge した。
- `src/main/aiChatService.ts` の conflict を解消し、main 側の app settings / user ID を使った datatype namespace 指示と、`@@` promptless continuation の Python block 生成指示を両立させた。
- `src/renderer/AIChatPanel.tsx` の conflict を解消し、main 側で分離された `AIChatSettingsDialog` を正として採用した。
- `AIChatSettingsDialog` に `@@ Promptless continuation` system prompt 欄を追加し、空欄チェックと説明文を 4 種類の prompt 前提へ更新した。
- main 由来の `docs/10_要求/20_ユーザー体験.md` trailing whitespace を 1 件修正した。

## 確認

- `npm run build` が成功した。
- `git diff --cached --check` が成功した。
- Vite の CJS Node API deprecation warning と chunk size warning は既存 warning として表示された。
