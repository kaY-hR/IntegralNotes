# 2026-04-10 22:38

## 背景

- git 管理下のファイルだけを外部共有用フォルダへコピーしたい要望があった。
- `docs/00_履歴`, `docs/10_要求/IntegralNotes.prj.md`, `Notes`, `.vscode`, `.codex`, `AGENTS.md` は共有対象から外したい意図が共有された。
- 除外リストは `.copyexclude` で管理し、このファイル自体と実行用 bat もコピー対象から外したい要件があった。

## 実施内容

- `.copyexclude`
  - repo 相対パスで除外対象を定義するファイルを追加した。
  - 指定された履歴、要求定義、Notes、`.vscode`、`.codex`、`AGENTS.md` に加え、`.copyexclude`、`copy-git-files.bat`、内部 helper `src/dev/export-tracked-files.ps1` も除外対象に入れた。
- `copy-git-files.bat`
  - repo 直下から実行できる入口バッチを追加した。
  - 引数なしでは `C:\Users\shimadzu\OneDrive - SHIMADZU\共有\test` へ出力し、必要ならコピー先を引数で上書きできる構成にした。
- `src/dev/export-tracked-files.ps1`
  - `git -c safe.directory=* ls-files` で追跡ファイルのみ列挙する export 処理を追加した。
  - `.copyexclude` の内容と内部除外対象をまとめて評価し、該当ファイルをコピー対象から外すようにした。
  - コピー先に `.copyexport-manifest` を保存し、前回出力にはあったが今回対象外になった stale file を削除するようにした。
  - コピー先が repo 内部にならないようにガードを追加した。

## 検証

- `copy-git-files.bat "C:\Users\shimadzu\.codex\memories\copy-export-test"`
  - `Copied: 69`, `Excluded: 69` で完了
  - `AGENTS.md`, `.copyexclude`, `copy-git-files.bat`, `src/dev/export-tracked-files.ps1`, `docs/10_要求/IntegralNotes.prj.md`, `docs/00_履歴/202604101428.md`, `.codex/skills/summarize/SKILL.md`, `.vscode/settings.json`, `Notes` が出力先に存在しないことを確認
- stale file cleanup
  - テスト出力先に `copy-git-files.bat` を意図的に残して manifest に追記した上で再実行し、`Removed stale files: 1` となり、再実行後に当該ファイルが消えることを確認
- `copy-git-files.bat`
  - 実共有先 `C:\Users\shimadzu\OneDrive - SHIMADZU\共有\test` へ実行し、`Copied: 69`, `Excluded: 69`, `Removed stale files: 0` で完了
  - 共有先でも除外対象が存在しないことを確認
