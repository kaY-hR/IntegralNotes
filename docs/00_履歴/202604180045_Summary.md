# 2026-04-18 Summary

## 依頼

- output dataset 名の `input type="text"` 上で text cursor が出ない

## 対応

- `src/renderer/styles.css` の `.integral-output-slot-row__name-input` に `cursor: text;` を追加した
- hover / focus 時も `cursor: text;` を維持するようにした
- `textarea` 由来で残っていた `resize` 指定は不要なので削除した

## 検証

- `npm run build` が通った
