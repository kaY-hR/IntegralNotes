# 2026-05-12 19:49 shimadzu-lc block 名称変更

## 実施内容

- shimadzu-lc の表示 block 名称を `Run Sequence` から `LC Linear Gradient` に変更した。
- plugin manifest と shimadzu-lc 専用 runtime block definition の title を揃えた。
- 内部 block type の `run-sequence` は既存ノート互換性のため維持した。

## 確認

- `npm run build`: OK
- `plugins/shimadzu-lc` で `npm run install:local`: OK
- `Run Sequence` の現行表示参照が残っていないことを確認した。
