# 2026-04-18 21:48

## 概要

- `Notes/src` のサンプル解析 block を、`json` と `plot` の 2 出力を持つ単一 block に再編した
- 併せて `implement-integral-block` skill の説明も、最新の output slot metadata 契約へ更新した

## 実装

- `Notes/src/lc_text_to_chromatogram_json.py`
  - block を `LC Text To Chromatogram` に更新
  - output slot を `json` と `plot` の 2 つに拡張
  - `plot` 出力の HTML 描画処理を内部へ併合
  - `json` は `project_to_inputs = ["source"]`
  - `plot` は `auto_insert_to_work_note = True` と `project_to_inputs = ["source"]`
- `Notes/src/chromatogram_json_to_plotly_html.py`
  - 独立 block 候補を重複させないため削除
- `Notes/.codex/skills/implement-integral-block/*`
  - output slot metadata と 2 出力 block の考え方を追記
  - `lc_text_to_chromatogram_json.py` / `chromatogram_pca.py` を現行サンプルとして参照に追加
- `Notes/.claude/skills/implement-integral-block/*`
  - `.codex` 側と同内容に更新

## 確認

- `python -m py_compile Notes/src/lc_text_to_chromatogram_json.py` 成功
- skill 配下に削除済み script 名が残っていないことを確認
- 検証で生成された `Notes/src/__pycache__` は削除済み
