# 2026-04-16 17:08

- Python callable 用の `integral` SDK を visible workspace package として `scripts/integral/` に移した。
- `scripts/integral/__init__.py` と `py.typed` を追加し、現在の workspace では app を起動しなくても `python scripts/demo_hello_report.py` のような `scripts/` 配下 script から `from integral import integral_block` が解決できるようにした。
- app main 側では、workspace 初期化時に bundled template から `scripts/integral/` を同期する処理を追加した。これにより packaged app でも任意 workspace に同じ package を配置できる。
- Python runner は app 内蔵 SDK path ではなく、workspace の `scripts/` を `sys.path` へ追加して user script を実行するように変更した。runtime と authoring の import 先を一致させた。
- packaging 設定は `package.json` の `build.extraResources` を `scripts/integral -> python-sdk/integral` に変更した。
- 旧 `plugin-sdk/python/` 下の Python package / pyproject / README は削除し、`plugin-sdk/README.md` は新しい方針に合わせて更新した。
- 関連設計文書 `docs/20_アーキテクチャ/30_Python汎用解析プラグイン.md`, `docs/30_設計/30_Pythonスクリプト登録と実行.md`, `docs/30_設計/35_ElectronからPythonを呼ぶ仕組み.md` を更新し、`scripts/integral/` と workspace 同期の仕組みに揃えた。
- `npm run build` は成功し、`python scripts/demo_hello_report.py` も import error なく終了した。
