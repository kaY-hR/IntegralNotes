# 2026-05-05 02:30

## 話したこと

- vendor plugin っぽい folder に `.py` を置き、bat で `%LocalAppData%` へ copy するだけで、今回の global external Python discovery の確認には十分と整理した。
- MVP では解析 script 用の manifest / registry は使わない方針を再確認した。
- `plugins/dummy-vendor` は IntegralNotes の runtime plugin ではなく、`%LocalAppData%/IntegralNotes/plugins` 配下に置かれた `.py` を `>` popup が拾えるかを見るための dummy sample とした。

## 実装

- `plugins/dummy-vendor/blocks/demo_report.py` を追加し、`@integral_block` 付きの dummy HTML report block を用意した。
- `plugins/dummy-vendor/install-localappdata.bat` を追加し、folder を `%LocalAppData%\IntegralNotes\plugins\dummy-vendor` へ copy するだけの installer にした。
- 当初 `robocopy` で展開していたが、exit code 16 で失敗したため、固定ファイルを `copy /Y` で明示的に配置する形へ変更した。
- `plugins/dummy-vendor/README.md` と `plugins/README.md` に、manifest なしの external Python discovery 確認用 sample であることを記載した。
- 当初追加していた runtime plugin 用の `integral-plugin.json` / `package.json` / host / renderer は削除した。

## 確認

- `npm run build` 成功。
- `git diff --check` は whitespace error なし。改行コードの CRLF 変換警告のみ。
