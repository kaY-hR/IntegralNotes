# 2026-04-08 19:09 作業ログ

## 依頼

- `UserReq` を元に作業開始
- まずは VSCode / Obsidian 的な基本機能を揃える
  - 実フォルダ読込
  - 上部メニューバー
  - フォルダを開く
  - 左サイドバーのツリーと操作導線
  - ツリー項目ごとの削除・リネームなど

## 対応

- `src/main/workspaceService.ts`
  - 固定デモノート自動生成をやめ、選択した実フォルダをワークスペースとして扱う構成に変更
  - 現在のワークスペースパスを保持できるようにし、前回開いたフォルダを復元する仕組みを追加
- `src/main/main.ts`
  - `workspace:openFolder` IPC を追加
  - Electron のフォルダ選択ダイアログからワークスペースを切り替えられるように変更
  - ウィンドウタイトルに現在のワークスペース名を反映
- `src/main/preload.ts`
  - renderer から `openWorkspaceFolder()` を呼べるように API を追加
- `src/shared/workspace.ts`
  - `IntegralNotesApi` に `openWorkspaceFolder` を追加
- `src/renderer/App.tsx`
  - 上部メニューバーを追加
  - `Open Folder / New Note / New Folder / Rename / Delete / Refresh / Save` の導線を追加
  - ワークスペース切替時に開いているタブをリセットし、新ワークスペースへ追従する処理を追加
  - サイドバー上部にワークスペース名と実パス表示を追加
- `src/renderer/FileTree.tsx`
  - ツリー各行に `+N / +F / Ren / Del` の直接操作を追加
  - 選択中アイテムに依存せず、行単位で作成・リネーム・削除できるように変更
- `src/renderer/styles.css`
  - 上部メニューバーの見た目を追加
  - サイドバーのワークスペース表示と、ツリー行アクションの UI を追加

## 検証

- `npm run build` 成功

## メモ

- 現状でもノート編集対象は `.md` 前提
- 実フォルダを開けるようになったので、次は必要に応じてネイティブメニューや非 markdown ファイル対応の範囲を詰める
