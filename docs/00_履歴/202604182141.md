# 2026-04-18 21:41

## 概要

- `Notes/` 側のサンプル解析スクリプトとローカル SDK を、直前に入れた output slot metadata 契約へ追従させた

## 更新内容

- `Notes/src/chromatogram_json_to_plotly_html.py`
  - `src/` 配下共通の SDK import 前処理を追加
  - `plot` output に `auto_insert_to_work_note = True` と `project_to_inputs = ["source"]` を追加
- `Notes/src/chromatogram_pca.py`
  - `report` output に `auto_insert_to_work_note = True` と `project_to_inputs = ["source"]` を追加
- `Notes/src/lc_text_to_chromatogram_json.py`
  - `src/` 配下共通の SDK import 前処理を追加
  - 中間生成物である `json` output は work note 自動挿入せず、`project_to_inputs = ["source"]` のみ追加
- `Notes/scripts/integral/__init__.py`
  - `IntegralSlotSpec` に `auto_insert_to_work_note` / `project_to_inputs` を追加
  - decorator 正規化処理でも上記 2 項目を扱えるようにした

## 確認

- `python -m py_compile` で対象 4 ファイルの構文確認を実施し、成功
- 検証で生成された `Notes/**/__pycache__` は削除済み
