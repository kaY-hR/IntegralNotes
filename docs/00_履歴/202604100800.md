# 2026-04-10 08:00

## 背景

- `shimadzu-lc` plugin への追加フィードバックとして、詳細モードとは別に、渡した画像を使うシンプルモードも選べるようにしたいという要望があった。
- あわせて、`start B.Conc / peak B.Conc / final B.Conc` のサマリー表示は不要との指摘があった。

## 実施内容

- `plugins/shimadzu-lc/renderer/index.html`
  - renderer 内ローカル state で `詳細` / `シンプル` を切り替える mode switch を追加。
  - `シンプル` モードでは `gradient.png` をそのまま使う画像ベースの簡易表示にした。
  - `詳細` モードでは従来どおり `analysis-time`, `time-prog`, `Start/End B.Conc` を編集できるように維持した。
  - `start B.Conc / peak B.Conc / final B.Conc` の summary card は削除し、sidebar は `Analysis Time` と `Points` のみ表示するように整理した。
  - モード切替は renderer 内の表示状態だけで管理し、block JSON には保存しない構成にした。
- `plugins/shimadzu-lc/README.md`
  - `詳細` / `シンプル` モードの違いを追記した。

## 検証

- `plugins/shimadzu-lc/renderer/index.html`
  - `<script>` 部分を `new Function(...)` でパース確認し、syntax error がないことを確認

## 補足

- zip の再生成は今回は未実施。
