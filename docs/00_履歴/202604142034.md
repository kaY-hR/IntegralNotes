# 2026-04-14 20:34

## 会話要約

- 直前の `Issue 20` 完了を踏まえて、`Issue 3` の文面も現在の frontmatter 方針に合わせて更新してほしい依頼に対応した。

## 実施したこと

- `update-issue` スキルの指針と `docs/Issues.md` の `Issue 3` 現状記載を確認した。
- `Issue 20` で frontmatter 隠蔽が `data-catalog` 限定ではなく `cwd` 配下 Markdown 全体の標準動作になったことを前提に、`Issue 3` の論点整理を更新した。
- `docs/Issues.md` の `Issue 3` を修正した。
  - `data-note だけが特殊` という前提を削除した。
  - `raw markdown` は current scope では `frontmatter を除いた本文 raw` と定義するのが自然、と明記した。
  - frontmatter を含む full-file raw 編集は別 Issue に分離した方がよい、と整理し直した。

## 補足

- この更新により、`Issue 3` は `Markdown tab 向けの body-only source editing mode` を扱う Issue として位置づけが明確になった。
