# 2026-04-16 10:45

## 会話概要

- ユーザー要望: `.idts` viewer 内で埋め込んだ note block 自体をクリックしたら、対応ノートを別タブで開けるようにしたい
- 既存の「ノートを開く」ボタンは残したまま、導線を増やしたい

## 実装内容

- `src/renderer/DatasetManifestFileViewer.tsx`
  - note preview を `role="button"` の surface で包み、クリックまたは Enter / Space で `requestOpenManagedDataNote(manifest.noteTargetId)` を発火するようにした
- `src/renderer/styles.css`
  - note preview surface に hover / focus style を追加
  - preview 内の pointer event を止めて、カード全体クリックで確実に open されるようにした

## 検証

- `npm run build` 成功
