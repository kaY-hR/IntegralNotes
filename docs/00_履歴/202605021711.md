# 2026-05-02 17:11 doc-review

## 依頼

- `doc-review` スキルを使い、`docs/10_要求` を基準に `docs/20_アーキテクチャ`、`docs/30_設計`、必要箇所の `src` 実装を照合した。

## 確認した範囲

- 最新履歴 3 件を確認した。
- 要求文書、アーキテクチャ文書、主要設計文書を確認した。
- `src/shared/integral.ts`、`src/main/integralWorkspaceService.ts`、`src/main/dataNote.ts`、`src/main/workspaceService.ts`、`src/renderer/integralCodeBlockFeature.tsx`、`src/renderer/MilkdownEditor.tsx` など、仕様差分に関係する実装を確認した。

## 主な指摘候補

- 実行済み block は仕様上「削除だけ可能」だが、実装と履歴では `Undo` が追加され、生成物削除と block 初期化を行う。
- data-note の解決キーについて、要求と実装は `noteTargetId` を扱う一方、アーキテクチャ/設計文書に `{id}.md` や data ID 起点の記述が残っている。
- Managed File の最小フィールドとして、アーキテクチャ文書に `datasetId` が混入している。
- Electron から Python を呼ぶ設計文書では decorator から読む情報に `params` が漏れているが、要求と実装では `params` schema が source of truth。
- Issue 30 の hidden file 除外方針は未解決で、現状の link/embed 補完は hidden 表示 ON の workspace tree に依存して hidden/system path が混ざりうる。

## 確認

- レビューと履歴作成のみ。build / test は実行していない。
