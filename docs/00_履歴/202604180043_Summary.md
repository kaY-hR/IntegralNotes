# 2026-04-18 Summary

## 依頼

- output dataset 名入力が `textarea` なのは不自然で、改行できる意味が分からない
- 常識的な単行入力にしたい

## 対応

- `src/renderer/integralCodeBlockFeature.tsx` の output 名入力を `textarea` から `input type="text"` に変更した
- dataset 名は単行の file stem 入力として扱う形に揃えた

## 検証

- `npm run build` が通った
