# 2026-04-16 17:52

- ユーザー依頼に基づき、`Issues.md` と `docs/00_履歴` を除く仕様文書群について、文書同士および `src/` 実装との整合性を監査した。
- 監査対象は `docs/10_要求/*.md`, `docs/20_アーキテクチャ/*.md`, `docs/30_設計/*.md` とし、`docs/10_要求/機能詳細イメージ/*.PNG` は今回除外した。
- まず最新履歴 3 件と要求文書群、`src/` 構成を確認し、直近で更新された Python SDK 同期、`.vscode/settings.json` 補助設定、`.idts` embed/viewer 仕様の履歴を把握した。
- 文書監査の結果、`docs/10_要求/20_ユーザー体験.md` の「original data を hidden `.store/` canonical path にしてよい」という記述が、`docs/10_要求/10_MVP要件.md` と `docs/20_アーキテクチャ/10_データモデル.md` の visible path 前提と矛盾していることを確認した。
- 実装監査の結果、hidden 表示を有効にすると `.store/.integral/data-notes/*.md` が通常の explorer 項目として露出し、rename / move / delete 対象に入るため、「data-note は system-managed で rename / move を許可しない」という仕様に反していることを確認した。
- また、Milkdown の link/embed 補完候補が `visibleWorkspaceEntries` 由来になっており、仕様上は `cwd` 配下の user-managed file を対象にすべきところ、hidden user-managed file は通常時に候補から落ち、逆に hidden 表示時は data-note が候補へ混入しうることを確認した。
- さらに、GC は仕様書側で要求されている一方、`src/main/integralWorkspaceService.ts` では再実行ごとに新しい `DTS-*` と runtime log を作成するのみで、参照切れ dataset や runtime log を回収する処理は未実装であることを確認した。
- 低優先度の差分として、workspace file viewer は仕様書の built-in viewer 対象拡張子より広く `.bmp/.gif/.webp/.htm` や多数の text 拡張子を扱っていること、また dataset metadata 正規化では legacy 互換のため `representation: "directory"` や旧 schema (`storeRelativePath`, `sourceMembers`) を受理しており、「未リリースのため旧 schema 互換を持たない」という文書方針とずれていることを確認した。
- `npm run build` は成功し、監査はコード変更なし・履歴追記のみで完了した。
