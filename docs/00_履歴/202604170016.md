# 2026-04-17 00:16

- `AI Chat Panel` 追加について、まずは実装ではなく調査・検討を優先する方針に切り替えた。
- 既存実装は `Activity Bar / Sidebar Host / Main Workspace` の 3 列構成で、`Explorer` と `Plugins` は built-in sidebar view として載っていることを再確認した。
- そのため AI Chat は通常の sidebar view にするより、Activity Bar の icon から main workspace 側の専用 tab を開く built-in command item として扱う方が、Claude Code / Codex / Copilot Chat に近い UX になると整理した。
- `docs/Issues.md` に新規 Issue 32 `Activity Bar から開ける AI Chat Panel を追加したい` を追加した。
  - MVP 範囲として、単一 chat tab、会話履歴、streaming 応答、cancel、active document / selected path を文脈として渡す方針を記載した。
  - provider / transport の第一候補を `Vercel AI Gateway + BYOK` とし、ただし transport abstraction 自体は `OpenAI-compatible endpoint` 差し替え可能にして lock-in を避ける方針を記載した。
  - `GitHub Copilot Extensions / SDK` は standalone Electron app の中核基盤には向かない、という判断も Issue に反映した。
- その後、`Claude Code / Codex` 的な使い方を目指すなら `chat UI` だけでは足りず、`text-read / grep / diff patch write` などの tool API が必須、という追加要件を整理した。
- `docs/Issues.md` の Issue 32 に想定ユースケースを追記した。
  - ユーザーは `cwd` に Markdown の実験ノートを作り、過去の実験ノート、実験結果、論文、特許を読みながら判断する、という前提を明記した。
- 併せて新規 Issue 33 `AI agent が workspace を読んで編集できる tool API を整備したい` を追加した。
  - `list_tree / grep_text / read_text / read_document / get_active_context / apply_patch / write_text` を最低限の API 候補として整理した。
  - PDF text extraction、frontmatter 保持、`.idts` の構造化 read、dirty tab と衝突しない保存契約、traceability など、IntegralNotes 固有の論点も列挙した。
- 調査途中で一時的に AI 用 shared type file を作り始めたが、ユーザー意図が planning のみに変わったため削除し、実装差分は残していない。
