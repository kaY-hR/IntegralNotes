# 2026-04-15 15:25

## 会話要約

- 別スレッド由来で混ざった `Issue 24` 実装がないかを点検し、仕様とズレていた data-note open 導線を修正した。
- 続けて、`path changed + hash changed` の候補選択 confirm を app 内 dialog として追加し、tracked path を更新できるようにした。
- 途中で設計壁打ちの割り込みが入ったが、このスレッドでは「一旦今の実装を進める」方針で継続した。

## 実装内容

- `src/renderer/workspaceOpenEvents.ts`, `src/renderer/IntegralAssetDialogs.tsx`, `src/renderer/integralCodeBlockFeature.tsx`, `src/renderer/App.tsx`
  - data-note open を hidden file path 直指定ではなく `targetId` 基点に揃えた。
  - hidden path (`.store/.integral/data-notes/{id}.md`) の解決は `App.tsx` に集約した。
- `src/shared/integral.ts`, `src/shared/workspace.ts`
  - shared contract から `dataNoteRelativePath` を外し、managed data tracking issue / resolve request の型を追加した。
- `src/main/preload.ts`, `src/main/main.ts`
  - tracking issue 一覧取得と resolve 用の IPC を追加した。
- `src/main/integralWorkspaceService.ts`
  - reconciliation を拡張し、`path changed + hash changed` 系で `basename` ベースの candidate を探すようにした。
  - candidate が一意なら自動追従し、複数なら pending issue として保持するようにした。
  - pending issue を resolve して metadata の `path / hash / visibility` を更新する経路を追加した。
- `src/renderer/ManagedDataTrackingDialog.tsx`, `src/renderer/styles.css`
  - recorded path / hash と candidate path 一覧を見ながら tracked path を選び直す dialog を追加した。
- `docs/Issues.md`
  - `Issue 24` に今回の修正内容と confirm 導線追加を追記した。

## 検証

- `npm run build` が通ることを確認した。

## 補足

- current の confirm 実装は `basename` ベースの candidate 発見に寄せている。
- `derived dataset` と `source dataset` の representation をどこまで揃えるか、manifest に `folder-path` を持たせるかは、まだ別途設計余地がある。
