# 2026-04-20 14:17

## 概要

- `Notes` 配下の `implement-integral-block` スキルを、repo 内の開発 docs 依存から切り離し、配布向けの自己完結型スキルへ差し替えた
- 対象は `Notes/.codex/skills/implement-integral-block` と `Notes/.claude/skills/implement-integral-block`

## 変更内容

- `SKILL.md`
  - 「IntegralNotes のプロダクト説明から入る」構成をやめた
  - `integral` SDK の表面 API と block 用 Python 実装に直接フォーカスする導線へ変更した
  - repo 直下の要求・設計・履歴 docs を読ませる構成をやめ、`Notes` 配下の配布物だけを根拠にするよう明記した
- `references`
  - `integralnotes-overview.md` と `integral-block-contract.md` を削除した
  - 代わりに下記を追加した
    - `integral-sdk-interface.md`
    - `block-implementation-patterns.md`
  - decorator が受け付ける引数、slot object の許可キー、拡張子正規化、bool 制約、`.idts` の入出力解釈を、`scripts/integral/__init__.py` と bundled examples ベースで整理した
- `agents/openai.yaml`
  - `.codex` 側の短い説明と default prompt から `IntegralNotes` 固有表現を外し、`integral SDK` ベースの block 実装ガイドへ更新した

## 意図

- 客先配布後も、この repo の開発文書がなくても成立するスキルにする
- LLM がまず把握すべき対象を
  - `from integral import integral_block`
  - `main(inputs, outputs, params)`
  - slot 定義
  - file / bundle path の扱い
  に限定する
- app 側の内部事情や hidden metadata へ不用意に依存しないようにする

## 確認

- `python C:\Users\shimadzu\.codex\skills\.system\skill-creator\scripts\quick_validate.py .\Notes\.codex\skills\implement-integral-block`
  - 成功
- `python C:\Users\shimadzu\.codex\skills\.system\skill-creator\scripts\quick_validate.py .\Notes\.claude\skills\implement-integral-block`
  - 成功
- `rg` で旧 repo docs 参照や旧 reference 名の残存がないことを確認した
