# 2026-04-27 23:03

- エクスプローラーパネルの右クリックメニューに「エクスプローラーで開く」を追加した。
  - workspace root の空白部メニューでは workspace root を開く。
  - file / directory のメニューでは対象 path を OS のファイルマネージャで開く。

- 本体トップメニューに「VSCodeで開く」ボタンを追加した。
  - workspace 未選択時は disabled にした。
  - 選択中 workspace を `vscode://file/...` URI で VSCode に渡す。

- `src/main/main.ts`, `src/main/preload.ts`, `src/shared/workspace.ts`, `src/renderer/App.tsx`
  - `workspace:openPathInFileManager` IPC と renderer API を追加した。
  - `workspace:openWorkspaceInVSCode` IPC と renderer API を追加した。
  - 既存の `openPathInExternalApp` は既定アプリ起動のまま残した。

- 検証
  - `npm run build` 成功。

- 補足
  - 最終確認時点で AI Chat 関連の未コミット変更も見えているが、今回の作業ではそこを戻していない。
