# 2026-04-16 23:34

- Activity Bar を app shell に追加し、レイアウトを `Activity Bar / Sidebar Host / Main Workspace` の 3 列へ変更した。
- Explorer は built-in sidebar view として移植し、既存の tree 操作、drag & drop、hidden toggle、rename/delete shortcut を維持した。
  - Explorer 専用 shortcut は Explorer view が active なときだけ効くようにした。
- Plugin 管理画面を modal dialog から built-in `Plugins` sidebar view へ移植した。
  - `Install ZIP`, `Refresh`, `Uninstall` はそのまま sidebar から実行できる。
  - plugin card に `sidebar views` 数と定義一覧も表示するようにした。
- plugin system に `sidebarViews` contribution を追加した。
  - `src/shared/plugins.ts` で manifest parse / installed definition / renderer message type を拡張した。
  - `src/main/pluginRegistry.ts` で `sidebarViews[].renderer.entry` を解決し、renderer document を読めるようにした。
  - preload / IPC に `loadPluginSidebarViewDocument(pluginId, sidebarViewId)` を追加した。
- renderer 側に external sidebar view host を追加した。
  - `src/renderer/ExternalPluginSidebarView.tsx` で plugin iframe を読み込み、`integral:set-sidebar-view` を postMessage するようにした。
  - Activity Bar 上の external sidebar item は `icon` または `title` から 1-2 文字の fallback label を表示する。
- 仕様文書を更新した。
  - `docs/30_設計/50_プラグイン定義.md`
  - `docs/30_設計/80_ActivityBarとSidebarView.md`
  - `docs/20_アーキテクチャ/20_ブロックとプラグイン.md`
  - `docs/10_要求/20_ユーザー体験.md`
  - `plugin-sdk/docs/開発者向けアナウンス.md`
- plugin SDK の manifest/schema と renderer helper も `sidebarViews` 前提へ更新した。
  - `plugin-sdk/schemas/integral-plugin.schema.json`
  - `plugin-sdk/src/manifest.d.ts`
  - `plugin-sdk/src/renderer.d.ts`
  - `plugin-sdk/src/renderer.js`
- 旧 modal 実装 `src/renderer/PluginManagerDialog.tsx` は削除した。
- `npm run build` が通ることを確認した。

補足:

- `git status` は repository ownership による `dubious ownership` 判定で実行できなかったため、変更確認は build と直接編集内容ベースで行った。
