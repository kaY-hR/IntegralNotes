# 2026-05-03 18:24 Inline Action の readScope と autosave 待機修正

## 背景

- `@@continue` で `writeWorkspaceFile` は成功しているのに、bash 側では `scripts/` や `Data/` が見えず、AI が「保存されていない」と誤診していた。
- `6166da8` の autosave 自体は残っていたが、inline action 送信前に保存完了を待っていなかった。
- 実運用で参照される skill は開発用 `.codex/skills` ではなく `Notes` 側なので、`Notes/.codex` と `Notes/.claude` を更新対象にした。

## 実施内容

- `@@continue` の `readScope` を `entire-workspace` に変更した。
  - `Notes/.inline-action/continue.md`
  - `src/main/aiChatService.ts` の fallback 定義
  - `docs/30_設計/40_InlineAction.md`
- inline action 開始時に trigger 削除後の Markdown を autosave queue に入れ、その保存完了を待ってから `submitInlineAction` するようにした。
  - `src/renderer/MilkdownEditor.tsx`
  - `@@continue` だけでなく `@@write` / `@@mkpy` / `??` / `>>` など inline action 全体で start/finish autosave を行う。
- `Notes` 側の `implement-integral-block` skill に以下を追記した。
  - 既存 `scripts/**/*.py` に使える `@integral_block` callable があれば優先して再利用または最小更新する。
  - `.idts` input slot は `datatype` だけでなく `extensions=[".idts"]` も必ず宣言する。
- 実ワークスペース `C:\Users\shimadzu\Documents\Notes` 側にも同内容を反映した。

## 確認

- `npm run build` 成功。
