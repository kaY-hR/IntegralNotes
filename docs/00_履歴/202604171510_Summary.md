# 2026-04-17 15:10 Summary

## 会話要約

- scrollbar 可視化の追加調整後、embed preview の中身が枠を貫通してはみ出す見た目崩れが報告された。
- 添付画像を確認したところ、前回 `surface` 側を `overflow: visible` に寄せた影響で、plugin/preview の大きい内容が card 外へ出ている状態だった。

## 実装

- `src/renderer/styles.css`
  - `editor-workspace-embed__surface` を `overflow: hidden` に戻し、embed 枠で内容をクリップするようにした。
  - `editor-workspace-embed__plugin` と `editor-workspace-embed__dataset` も `overflow: hidden` に戻した。
  - embed 内 plugin iframe の `min-height` を 0 に寄せ、親高さを超えて押し出しにくいようにした。
  - loading shell だけは `min-height: 100%` にして、空状態でも embed 高さに収まるようにした。

## 確認

- `npm run build` : 通過

## 補足

- 今回は「枠を貫通しないこと」を優先してクリップを戻した。
- scrollbar 可視性との兼ね合いは、実機で再確認して必要なら viewer 種別ごとに追加調整する。
