# 2026-04-10 06:49

## 背景

- `docs/20_アーキテクチャ/pluginシステム.md` の次段階が store 実装先行になっていた。
- ただし現時点で優先すべきなのは store ではなく、plugin renderer を read-only preview 前提から外し、専用 GUI の編集結果を `itg-notes` JSON に反映する仕組みの確立だと判断した。

## 実施内容

- `docs/20_アーキテクチャ/pluginシステム.md`
  - plugin store を後段に下げ、`interactive renderer` と block JSON 同期を先にやる方針へ更新。
  - `interactive renderer に向けた方針` を追加。
  - `integral:set-block` に加え、plugin renderer から app 本体へ `integral:update-params` を返す reverse bridge の MVP を追記。
  - source of truth を Markdown 上の `itg-notes` JSON とし、plugin renderer の編集結果は app 本体 node view が再 serialize して保存する前提を明記。
  - 次段階を `reverse bridge -> 専用 GUI 編集 -> validation/normalization -> schema/snippet -> host runtime -> store` の順へ並べ替えた。
- `plugin-sdk/docs/開発者向けアナウンス.md`
  - 開発者向けにも、renderer は preview-only 固定ではなく、今後は `integral:update-params` で `params` 編集 UI を持つ方針を追記。
- `docs/20_アーキテクチャ/jsonスキーマ.md`
  - `itg-notes` JSON が block の唯一の正であり、plugin GUI の編集結果もここへ反映することを明記。
  - plugin renderer が当面更新する対象は `params` を基本とする前提を追記。

## 判断

- まず必要なのは plugin renderer からの reverse bridge であり、store はその後でよい。
- full block 差し替えより先に `params` 更新に絞る方が、安全に MVP を作りやすい。
- `type` や manifest 由来情報は app 側が保持し、plugin renderer には block 固有 UI の責務を寄せる。

## 検証

- 文書更新のみ。ビルドや GUI 確認は未実施。
