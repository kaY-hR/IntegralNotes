# 2026-04-28 02:22

- `$doc-review` に従い、`docs/10_要求` を基準に `docs/20_アーキテクチャ`、`docs/30_設計`、必要箇所の `src` 実装を照合した。
- 直近履歴として `202604280203.md`、`202604280149.md`、`202604272310.md` を確認した。
- 主な確認軸:
  - managed file / dataset / data-note
  - block schema / slot I/O / Python 実行
  - note link / image / hidden data-note 補完
  - Activity Bar / Search / Sidebar 周辺
- 主な指摘候補:
  - non-`.idts` output path は要求上は指定 path をそのまま使うが、実装は `.idts` 以外の scalar `out:` を output config 化せず、既定の `/Data/{slot}` 側で生成する挙動になっている。
  - `docs/30_設計/10_ブロックJSON設計.md` と `docs/20_アーキテクチャ/00_全体アーキテクチャ.md` に `auto` / `outputConfigs` / datasetId 時代の block source 設計が残り、現在の path-based slot I/O 方針と混在している。
  - `docs/30_設計/35_ElectronからPythonを呼ぶ仕組み.md` は output を dataset manifest 前提で説明しており、`30_Pythonスクリプト登録と実行.md` および要求側の non-`.idts` output 仕様と食い違う。
  - Milkdown link/embed 補完候補は explorer の hidden 表示状態に依存しており、hidden 表示時に `.store/.integral/data-notes/*.md` が候補に混ざりうる。これは Issue 30 としても未完了。
- 実装変更は行っていない。
