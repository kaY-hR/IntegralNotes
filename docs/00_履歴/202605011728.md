# 2026-05-01 17:28 developer README for Playwright MCP

## 背景

- Playwright MCP と worktree-local dev port は仕様というより開発者向け運用手順なので、root `README.md` に載せる方が見つけやすい。

## 対応

- `README.md` に開発者向け起動手順を追記した。
  - `npm install`
  - `npm run build`
  - `npm run dev`
- `.integralnotes-dev.local.json` の役割を説明した。
  - worktree ごとの Vite renderer port
  - Playwright MCP の artifact dir / userData dir
  - 複数 worktree 間の `5173` reuse 回避
- Codex Playwright MCP の登録/解除手順を追記した。
  - `scripts\register-codex-playwright-mcp.bat`
  - `scripts\unregister-codex-playwright-mcp.bat`
- 詳細設計として `docs/30_設計/95_PlaywrightMCP.md` を参照する形にした。

## 確認

- ドキュメント更新のみ。build / 実行確認は未実施。
