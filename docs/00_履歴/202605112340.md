# 2026-05-11 23:40 shimadzu-lc グラジエント入力追加

## 実施内容

- shimadzu-lc のシンプル表示で、横軸 input を 2 つ追加した。
  - 最終濃度に切り替える時刻
  - 分析終了時刻
- 最終濃度に切り替える時刻は、初期濃度維持時間からの相対値ではなく、横軸上の絶対時刻として扱い、内部の gradient-time と同期するようにした。
- 分析終了時刻は runtime パラメータ `analysis-time` に反映するようにした。
- shimadzu-lc block の method input で読み込む拡張子を `.lcm` に変更した。
- README の記述を、6 つの軸 input と `.lcm` input に合わせて更新した。

## 確認

- renderer script の構文確認: OK
- `npm run build`: OK
- `plugins/shimadzu-lc` で `npm run install:local`: OK
- `git diff --check`: OK（既存の CRLF 変換 warning のみ）
