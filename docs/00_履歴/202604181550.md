# 2026-04-18 15:50 Python block 例と skill 更新

- `Notes/src/*.py` を確認し、旧来の dataset-directory 前提が残っていたため、slot 定義と I/O 契約を現行方針へ更新した。
- `Notes/src/chromatogram_json_to_plotly_html.py` は `chromatogram/json -> report/html` の単体 file I/O へ変更した。
- `Notes/src/chromatogram_pca.py` は入力を単体 `.json`、出力を `.idts` bundle に整理した。
- `Notes/src/lc_text_to_chromatogram_json.py` は入力を `.idts` bundle、出力を単体 `.json` に整理した。
- 併せて root 側の `scripts/integral/__init__.py` を更新し、`integral_block` decorator が slot object form (`name`, `extension`/`extensions`, `format`) を受けられるようにした。
- `scripts/demo_hello_report.py`, `scripts/demo_dataset_report.py`, `Notes/demo_dataset_report.py` も現行 slot 契約へ寄せた。
- `Notes/.codex/skills/implement-integral-block` と `Notes/.claude/skills/implement-integral-block` を更新し、
  - 「全 slot は dataset directory path」という旧説明を削除
  - `path-based slot I/O`
  - `non-.idts = direct file path`
  - `.idts = resolved bundle directory`
  - slot object form の decorator 記法
  へ差し替えた。
- `python -m py_compile` で変更した Python file 群の構文チェックは成功した。
