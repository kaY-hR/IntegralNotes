# 2026-04-17 08:24

- `integral_block` を使った IntegralNotes の解析 block 実装を Claude Code / Codex に説明するための skill を追加した。
- 追加先:
  - `Notes/.codex/skills/implement-integral-block/`
  - `Notes/.claude/skills/implement-integral-block/`
- skill の入口 `SKILL.md` では、最初に IntegralNotes を `note-first / block-based / results-focused` な ELN と説明してから、`integral_block` 実装へ入る流れを定義した。
- 詳細は `references/` に分離した。
  - `integralnotes-overview.md`
    - IntegralNotes の性質
    - `original data` と `dataset`
    - `.idts` と runtime path 解決
    - AI が block 実装で何を最適化すべきか
  - `integral-block-contract.md`
    - decorator / discovery / runtime contract
    - `run: relative/path.py:function` 前提
    - `inputs / outputs / params` の callable 署名
    - よくある誤り
- Codex 側には `agents/openai.yaml` も追加し、skill 一覧で識別しやすい metadata を付けた。
- `Notes/demo_hello_report.py`, `Notes/demo_dataset_report.py`, `Notes/scripts/integral/__init__.py`, `Notes/scripts/integral/README.md` と、要求・設計文書を skill から参照する構成にした。
- `quick_validate.py` で `.codex` / `.claude` 両方の skill を検証し、いずれも `Skill is valid!` を確認した。
