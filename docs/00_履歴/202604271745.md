# 2026-04-27 17:45

- `Notes` 配下に置いた Codex / Claude 用 skill を、workspace (`cwd`) 初期化時に root 直下へ自動配置したい依頼に対応した。

- `src/main/workspaceService.ts`
  - `ensureWorkspaceReady()` から、`Notes/.codex/skills -> .codex/skills` と `Notes/.claude/skills -> .claude/skills` の bootstrap 同期を呼ぶようにした。
  - 同期は `fs.cp(..., { recursive: true, force: true })` により一方向で上書き反映しつつ、削除同期はしない。
  - `ensureWorkspaceReady()` は頻繁に呼ばれるため、同一 workspace root では process 中 1 回だけ同期する guard を追加した。

- `docs/30_設計/86_AIChatPanelとAgentTooling.md`
  - app 内 skill runtime は root `.codex/skills` を読むこと、
    その前段で `Notes/.codex/skills` / `Notes/.claude/skills` を bootstrap 同期することを追記した。

- `docs/30_設計/20_ワークスペースレイアウト.md`
  - workspace layout に `.codex/` と `.claude/` を追記し、
    `Notes` 配下 skill から root 配置へ同期する bootstrap 方針を明記した。

- 検証
  - `npm run build` が通ることを確認した。
  - 一時 workspace を作って `WorkspaceService.ensureWorkspaceReady()` を実行し、
    `Notes/.codex/skills/sample-skill/SKILL.md` と `Notes/.claude/skills/sample-skill/SKILL.md` が
    root `.codex/skills/...` / `.claude/skills/...` へ複製されることを確認した。
