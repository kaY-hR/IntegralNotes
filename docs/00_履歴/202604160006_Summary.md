# 2026-04-16 00:06 Summary

## 会話要約

- 前回入れた `![` 埋め込み補完が期待どおり動かず、source が崩れることと、`![](相対パス)` にしても embed preview にならないことが報告された。
- 2 文字 trigger が扱いづらいなら `!` 単体でもよい、という条件緩和があった。

## 修正

- `src/renderer/MilkdownEditor.tsx`
  - embed 候補を mouse で選んだとき、誤って通常 link 挿入 (`insertWorkspaceLink`) に流れていた不具合を修正した。
  - embed 挿入を Markdown 文字列の `insert(markdown)` ではなく、Milkdown の `image` node を直接 `replaceWith(...)` する実装へ変更した。
  - これにより保存 source が通常の `![](/path)` に寄り、preview も image node view 経由で安定して出る構成にした。
  - `![` に加えて、行頭や空白直後の `!` 単体でも workspace file 補完 popup を開けるようにした。
- `docs/Issues.md`
  - `Issue 26` に 2026-04-16 修正メモを追記した。

## 確認

- `npm run build` : 通過

## 補足

- GUI 実機での手動確認までは今回は行っていない。
- 期待挙動は、候補選択後の source が `![](/relative/path)` になり、WYSIWYG 上では image node view から `html / svg / text / data-note` preview が出ること。
