# 2026-04-17 16:20

- original-data の自動登録と、missing managed data の cleanup 周りを一通り詰めた。

- `src/main/integralWorkspaceService.ts`
  - 起動時または Sync 時に `cwd` を scan し、未管理かつ非 `.md` の workspace file を `ORD-...` 付きで自動登録するようにした。
  - hidden directory 配下、hidden file、system-managed directory 配下、既存 managed path と衝突する path は自動登録対象から除外するようにした。
  - `path` / `hash` の両方で追跡できない managed data を `missing` issue として扱い、confirm 後に管理対象から外せるようにした。
  - original data を管理対象から外したとき、対応する source dataset の `.idts` manifest から `memberIds` を除去し、dataset metadata を再同期するようにした。
  - app 内 delete で managed data を消した場合は、対応する metadata JSON と data-note も即時 cleanup するようにした。
  - `visibility` 判定を調整し、`.` 始まりに加えて `_` 始まり directory も hidden として扱うようにした。ただし `_` 始まり file 自体は hidden にしない。

- `src/renderer/App.tsx`
  - Explorer の hidden 判定を directory-aware に見直し、`_` 始まり folder を hidden toggle の対象へ含めた。
  - missing managed data には `管理対象から外す` confirm を表示し、relink issue とは分離した。
  - Explorer の削除 confirm に、managed data を含む場合は metadata / data-note 削除と `.idts` 更新が伴うことを明示する文言を入れた。

- 文書更新
  - `docs/10_要求/10_MVP要件.md`
  - `docs/10_要求/20_ユーザー体験.md`
  - `docs/30_設計/20_ワークスペースレイアウト.md`
  - 自動登録、missing 時の confirm と cleanup、`.idts` 更新、`_` 始まり directory の hidden 判定を反映した。

- 検証
  - `npm run build` が通ることを確認した。
