# 2026-04-14 13:42

## 会話要約

- `Issue 10` として、data-note の metadata を frontmatter 化し、app では本文だけを表示・編集するようにしたい要望に対応した。
- 併せて、初回ワークスペース設定時に `Notes/` を自動作成しないこと、Markdown を開いただけで dirty 扱いになりにくくすることも対応した。
- 別件として、`CNK- -> DTS-`, `BLB- -> ORD-` の ID prefix 変更と、dataset 作成時の data-note 自動生成を新しい Issue として積んだ。

## 実施したこと

- `src/main/originalDataNote.ts` を追加し、以下を実装した。
  - legacy な metadata 箇条書き形式から frontmatter + 本文への移行
  - data-note frontmatter と本文の分離
  - 本文だけを保持したまま frontmatter を更新する処理
- `src/main/workspaceService.ts` を更新し、以下を変更した。
  - `data-catalog` 配下の data-note は frontmatter を隠して本文だけを editor / viewer に渡す
  - 保存時は既存 frontmatter を保持したまま本文だけを差し替える
  - metadata 同期時は本文を保持し、内容差分があるときだけ file を書き換える
  - 初回ワークスペース設定時の `Notes/` 自動作成をやめる
- `src/main/integralWorkspaceService.ts` を更新し、original data 登録直後に生成する data-note も同じ frontmatter 形式へ統一した。
- `src/renderer/MilkdownEditor.tsx` を更新し、editor 初期化前の `markdownUpdated` を拾わないようにして、開いただけで dirty が付きやすい挙動を抑えた。
- 文書を更新した。
  - `docs/Issues.md` で `Issue 10` を completed に更新
  - `docs/Issues.md` に `Issue 14`, `Issue 15` を追加
  - `docs/10_要求/10_MVP要件.md`, `docs/20_アーキテクチャ/10_データモデル.md`, `docs/30_設計/20_ワークスペースレイアウト.md` を現実装に合わせて更新

## 確認結果

- `npm run build` は成功した。

## 追加した Issue

- `14. dataset / original data の ID prefix を DTS- / ORD- に変更したい`
- `15. dataset 作成時も data-catalog に data-note を生成したい`
