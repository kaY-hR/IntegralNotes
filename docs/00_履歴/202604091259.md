## 2026-04-09 12:59

- 相談内容
  - 独自 UI 実装の導入以降、Milkdown 全体が不安定になった。
  - `itg-notes` 以外まで影響し、LaTeX preview も表示されなくなった。

- 原因整理
  - `code_block` node view を独自実装で差し替える方式は、Milkdown / Crepe 標準の CodeMirror, LaTeX preview と干渉しやすく、初期化順や fallback 処理によってハングや表示崩れを起こしていた。

- 対応
  - `src/renderer/MilkdownEditor.tsx`
    - custom node-view feature の導入をやめ、Crepe 標準の editor 構成へ戻した。
  - `src/renderer/integralSnippetMenu.ts`
    - 独自 preview は `Crepe.Feature.CodeMirror.renderPreview` で `itg-notes` のみ処理する形へ変更した。
  - `src/renderer/integralCodeBlockFeature.tsx`
    - React node view ではなく、静的 HTML preview を返す helper に整理した。
  - `src/renderer/integralBlockRegistry.tsx`
    - 独自 UI 対象言語を `itg-notes` のみに限定する判定を維持した。

- 期待される状態
  - `itg-notes` は Integral preview を表示する。
  - それ以外の通常 code block は Milkdown / Crepe 標準挙動に戻る。
  - LaTeX は標準の KaTeX preview に戻る想定。

- 確認
  - `npm run build` 成功。
  - 手動 UI 確認は未実施。
