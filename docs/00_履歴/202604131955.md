# 2026-04-13 19:55

## 会話要約

- 追加キューについて、エクスプローラーの tree 構造側にも `.md` フィルタが掛かっていそう、という補足があった。
- `shimadzu-lc migration` について、未変換部分があるなら最後まで実装してほしい依頼があった。

## 確認したこと

- explorer のフィルタは `src/main/workspaceService.ts` の `isVisibleEntry()` にあり、
  - `.` 始まりを除外
  - file は `.md` のみ表示
  になっている。
- ノート読込も同ファイルの `readNote()` で Markdown 限定になっている。

## 実施内容

- plugin manifest の block type 制約を新設計へ合わせた。
  - `src/shared/plugins.ts`
    - external plugin の `type` は `namespace.` prefix 固定ではなく、`run-sequence` のような stable ID でも許容するよう更新。
- legacy `shimadzu.lc / LC.Method.Gradient` を runtime 上で正規化するようにした。
  - `src/main/pluginRegistry.ts`
    - 旧 manifest を読んでも `shimadzu-lc / run-sequence` として扱う正規化を追加。
- integral block catalog 生成を新しい plugin id / block type 前提に整理した。
  - `src/main/integralWorkspaceService.ts`
    - `shimadzu-lc / run-sequence` 用の slot 定義を正式な external plugin block として生成。
    - old/new の重複があっても catalog 上で同一 block を二重に積まないよう整理。
- bundled plugin source 自体も新 manifest に移行した。
  - `plugins/shimadzu-lc/integral-plugin.json`
    - `id: "shimadzu-lc"`
    - `namespace: "shimadzu-lc"`
    - `blocks[0].type: "run-sequence"`
  - `plugins/shimadzu-lc/README.md`
    - 新 block type と新 package artifact 名へ更新。
- `standard-graphs` を現行構成から除去した。
  - `package.json`
    - `plugins:package/install/uninstall:*` から `standard-graphs` を除去。
  - `plugins/README.md`
    - `standard-graphs` の説明とコマンドを削除。
  - `plugins/standard-graphs/*` source を削除。
  - `plugins/dist/integralnotes.standard-graphs` を削除。
- 追加キューを補足した。
  - `docs/Issues.md`
    - explorer tree 側にも `.md` 限定フィルタがある旨を追記。

## 確認

- `npm run build`
  - 成功
- `npm run plugins:package:shimadzu-lc`
  - 成功
  - `plugins/dist/shimadzu-lc/shimadzu-lc-0.1.0.zip` を生成
- `node src/dev/plugin-installer.mjs install plugins/shimadzu-lc --target-root .tmp/plugin-install-root`
  - 成功
- temp install root を `PluginRegistry` で読ませ、
  - plugin id が `shimadzu-lc`
  - block type が `run-sequence`
  で見えることを確認

## 補足

- explorer / note tab 側の非 Markdown 表示はまだキュー段階で、今回は実装していない。
- ただし、どこにフィルタがあるかは特定済み。
