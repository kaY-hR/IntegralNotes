# 2026-04-14 17:07

## 会話要約

- 別件キューとして、frontmatter 分離を `data-catalog` 限定ではなく `cwd` 全体へ広げたい要望を整理した。
- 続けて、dataset の data-note 本文に canonical data へのリンクを入れたい要望を整理した。

## 実施したこと

- `src/main/workspaceService.ts` を確認し、frontmatter の本文分離が `isDataCatalogNotePath(...)` 条件付きで `data-catalog` 配下に限定されていることを確認した。
- `docs/Issues.md` に `Issue 20` を追加した。
  - title: `frontmatter 分離を data-catalog 限定ではなく cwd 全体の標準動作にしたい`
  - 内容: Markdown 全体での frontmatter / 本文分離、保存時保持、`data-note` 専用 metadata と一般 frontmatter の両立整理
- `docs/Issues.md` に `Issue 21` を追加した。
  - title: `dataset の data-note 本文に canonical data へのリンクを入れたい`
  - 内容: dataset data-note 本文から `.store` 側実体へ辿れるリンク表現の導入、source / derived dataset での一貫化、link 記法の整理

## 補足

- 今回は Issue 追加のみで、実装変更や build / test は行っていない。
