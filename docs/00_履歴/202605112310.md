# 2026-05-11 23:10 shimadzu-lc シンプル表示の上部整理

## 依頼

- `shimadzu-lc` のグラジエント renderer から上部の「グラジエント」見出しと `詳細|シンプル` 切替をなくす。
- 詳細機能は廃止し、シンプル版のみでよい。
- 縦幅を減らすため、実行ボタンを上部に移動する。

## 対応

- `plugins/shimadzu-lc/renderer/index.html`
  - renderer をシンプル表示固定に変更。
  - `詳細|シンプル` 切替、詳細モード描画、詳細編集テーブルの描画ロジックを削除。
  - 下部の結果確認表示と提案カードを削除。
  - `装置操作を実行` ボタンをグラフ上部右側の toolbar に移動。
  - 余白を詰めて、軸ラベル input と画像の表示領域を維持。
- `plugins/shimadzu-lc/README.md`
  - renderer がシンプル表示のみであること、実行ボタンが上部にあることを反映。

## 確認

- renderer 内 script の構文チェック成功。
- `npm run build` 成功。
- `plugins/shimadzu-lc` で `npm run install:local` 成功。
