# 2026-04-21 15:34 AI Chat 設計文書の実装追随

## 背景

- ユーザーから、この branch の AI Chat 実装を元に仕様文書を更新したい、という依頼あり。
- 既存の `docs/30_設計/86_AIChatPanelとAgentTooling.md` は、初期設計時点の「first version 想定」が強く、現在の実装状態よりも先の内容が混ざっていた。
- `MCP`, `streaming`, `useChat transport`, `OverlayFs` 前提など、現状と読後印象がズレる箇所があった。

## 更新内容

- `docs/30_設計/86_AIChatPanelとAgentTooling.md`
  - 文書全体を current branch 基準で再整理。
  - 実装済み:
    - `Activity Bar -> FlexLayout tab`
    - renderer / main process 分離
    - direct provider 優先 + AI Gateway fallback
    - `bash-tool` + `.codex/skills`
    - `readWorkspaceImage`
    - `renderWorkspaceDocument`
    - `writeWorkspaceFile`
    - tool message transcript
    - proxy env 対応
    - empty assistant text fallback
  - 未実装:
    - streaming / cancel
    - host command tool
    - MCP registry / add UI
    - approval UI
  - `OverlayFs` ではなく current branch は snapshot preload 方式であることを明記。
  - `MCP` は「設計のみ、未配線」であることを明記。

- `docs/30_設計/80_ActivityBarとSidebarView.md`
  - workspace-tool plugin items に `builtin:ai-chat` を追加。
  - `builtin:process-chain-viewer` と並ぶ built-in main tab opener として整理。
  - 途中で意味が切れていた一文を削除。

## 補足

- 今回は docs 更新のみで、`src` の挙動自体は変えていない。
