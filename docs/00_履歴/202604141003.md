# 2026-04-14 10:03

## 会話要約

- `docs/Issues.md` の task 8 に着手した。
- 非 Markdown ファイルは Milkdown を使わず、独立した実装にしたいという方針が確認された。
- 途中で要件が整理され、`json / txt / csv / log / xml / yaml / py` などの text 系だけ編集可能、`html / svg` は preview-only で十分、という扱いに確定した。

## 実施内容

- workspace file API を拡張した。
  - `src/shared/workspace.ts`
    - `WorkspaceFileDocument` に `previewBaseHref` を追加。
    - `WorkspaceFileViewKind` に `svg` を追加。
    - 非 Markdown text 保存用の `saveWorkspaceTextFile()` を追加。
  - `src/main/main.ts`
  - `src/main/preload.ts`
    - `workspace:saveTextFile` IPC を追加した。
- workspace file の分類と保存条件を整理した。
  - `src/main/workspaceService.ts`
    - `html` は raw source を返しつつ preview 用 `base href` を返すようにした。
    - `svg` は `image` ではなく独立 kind として扱い、preview-only 表示できるようにした。
    - `json / txt / csv / log / xml / yaml / py / ts / css / ps1` など text 系拡張子を `text` として扱うようにした。
    - `saveWorkspaceTextFile()` は `text` kind のみ保存可にした。
- renderer 側を Markdown と非 Markdown で分離した。
  - `src/renderer/App.tsx`
    - open tab state を `markdown / editable text / preview-only` に整理した。
    - `Ctrl+S` と dirty 表示を plain text tab にも適用した。
    - `html / svg / image / binary` は preview-only のままにした。
  - `src/renderer/WorkspaceTextEditor.tsx`
    - Milkdown 非依存の plain text editor を追加した。
  - `src/renderer/WorkspaceFileViewer.tsx`
    - raw `html` へ `<base>` を注入して preview するようにした。
    - `svg` を iframe preview できるようにした。
  - `src/renderer/styles.css`
    - plain text editor 用スタイルを追加した。
- `docs/Issues.md`
  - task 8 を完了済みに更新し、`html / svg` は preview-only でよい、という確定仕様へ揃えた。

## 確認

- `npm run build`
  - 成功

## 補足

- file 作成 UI は引き続き Markdown note 前提のまま。
- 今回の対応は「既存の text 系ファイルを開いて編集・保存する」経路に限定している。
