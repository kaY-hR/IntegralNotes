# 2026-04-13 15:01

## 背景

- `blob / chunk / block` ベースの新設計に合わせて、既存の `type` 単独 block と外部 plugin renderer 前提の実装を差し替える必要があった。
- 今回は特に
  - `general-analysis` plugin
  - `core-display` block
  - `.py-scripts/`, `blob/`, `.chunk/` の永続化
  を先に通すことを目的に実装した。

## 実装したこと

### main / preload / shared

- `src/shared/integral.ts`
  - block schema, asset catalog, Python script 登録、chunk inspection、block 実行 result の共通型を追加
- `src/main/integralWorkspaceService.ts`
  - blob import
  - source chunk 生成
  - Python script 資産登録
  - block catalog 生成
  - `general-analysis` の Python 実行
  - `core-display` 用 chunk inspection
  を追加
- `src/main/main.ts`, `src/main/preload.ts`
  - 上記機能の IPC を追加
- `src/main/workspaceService.ts`
  - `Notes`, `Artifacts`, `blob`, `.chunk`, `.py-scripts` を常に用意するように変更

### renderer

- `src/renderer/integralPluginRuntime.ts`
  - 外部 plugin manifest 読込ではなく、main の block catalog を見る形へ変更
- `src/renderer/integralBlockRegistry.tsx`
  - block schema を `plugin / block-type / params / inputs / outputs` 前提へ変更
- `src/renderer/integralSnippetMenu.ts`
  - `core-display` と登録済み Python script から snippet を生成
  - `Python Script を登録` 項目を追加
- `src/renderer/integralCodeBlockFeature.tsx`
  - input slot への chunk/blob 割当
  - `general-analysis` 実行
  - `core-display` の標準表示
  を node view 内で扱う実装へ変更
- `src/renderer/IntegralAssetDialogs.tsx`
  - chunk picker / blob picker / renderable viewer を追加
- `src/renderer/PythonScriptDialog.tsx`
  - Python script 登録ダイアログを追加
- `src/renderer/App.tsx`
  - `Import Files`, `Import Folders`, `Python Scripts` を top bar に追加
  - script 登録後に editor catalog を再読込するよう変更
- `src/renderer/styles.css`
  - 新しい dialog / slot / renderable UI のスタイルを追加

## 補足

- `npm run build` は成功
- `git status` は repository ownership の都合で取得できなかった
