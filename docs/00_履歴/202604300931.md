# 2026-04-30 09:31 inline AI 挿入の疑似 streaming 表示

## 目的

`??` inline AI 挿入で、LLM/tool の結果取得後に即時一括挿入されるため、挿入直前に文字送り風の表示を挟んで体感上 streaming 的に見せる。

## 対応内容

- `src/renderer/MilkdownEditor.tsx` に inline AI 挿入プレビュー用の文字送り helper を追加した。
- `insertMarkdownAtCursor` の結果取得後、popup の assistant streaming 表示に挿入 Markdown を数文字ずつ表示するようにした。
- 文字送り完了後は、従来どおり transcript 前置き済み Markdown を一括挿入する。
- main 側の AI runtime / tool loop / stream event contract は変更していない。

## 確認

- `npm run build` が成功した。
- Vite の CJS Node API deprecation warning と chunk size warning は既存の build warning として表示された。
