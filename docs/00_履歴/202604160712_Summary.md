# 2026-04-16 07:12 Summary

## 会話要約

- 画像 embed だけは custom preview ではなく、Milkdown 標準の画像機能を使えるようにしたい、という追加要望があった。

## 実装

- `src/renderer/workspaceEmbedFeature.tsx`
  - image / image-block node view を bridge 化し、workspace path が非画像のときだけ custom embed renderer を使うようにした。
  - 通常画像拡張子の target は Milkdown 標準の inline / block image view に委譲するようにした。
  - 以前の custom image resize で残る可能性がある `#integral-embed-height=NNN` は、標準画像へ流れるノードでは自動で除去するようにした。

## 確認

- `npm run build` : 通過

## 補足

- GUI 実機での画像選択 UI や block / inline の見え方までは今回は未確認。
