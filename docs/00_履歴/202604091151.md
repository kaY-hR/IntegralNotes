# 2026-04-09 11:51 Summary

## 概要

- `itg-notes` 対応後に再度ハングする問題に対処した
- 原因候補だった標準 code block view の取得ロジックをさらに単純化した
- リポジトリ内サンプルノートも `itg-notes` fence に揃えた

## 実施した対応

- `src\renderer\integralCodeBlockFeature.tsx`
  - `nodeViewCtx` を経由した標準 view 探索をやめた
  - `node_modules` 内の Milkdown 標準 `LanguageLoader` / `CodeMirrorBlock` を直接使って、非 `itg-notes` 用 node view を構築する形に変更した
- `Notes\Experiments\2026-04-08.md`
  - サンプル fenced block を `json` から `itg-notes` に変更した

## 検証

- `npm run build` 成功

## 補足

- これで非 `itg-notes` は Milkdown 標準 code block、`itg-notes` は custom view の分岐を lookup なしで処理する
- リポジトリ内の既存サンプルノートを開いた場合も、独自UIの確認ができる状態になった
