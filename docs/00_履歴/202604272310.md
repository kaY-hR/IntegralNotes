# 2026-04-27 23:10

- AI Chat Panel の会話履歴を app `userData` 配下へ永続化し、過去 session を復元して続きから会話できるようにした。

- `src/shared/aiChat.ts`, `src/shared/workspace.ts`
  - AI Chat の session summary / session / history snapshot 型を追加した。
  - `getAiChatHistory`, `createAiChatSession`, `saveAiChatSession`, `switchAiChatSession`, `deleteAiChatSession` 用の API 型を追加した。

- `src/main/aiChatService.ts`, `src/main/main.ts`, `src/main/preload.ts`
  - `app.getPath("userData")/ai-chat-history.json` を使う履歴ストアを追加した。
  - active session が無い場合は空 session を自動作成し、panel 起動時に復元できるようにした。
  - session は最大 50 件を保持し、`updatedAt` 順で管理する。
  - 保存時は message / attachment / diagnostics / tool trace を正規化して保持する。

- `src/renderer/AIChatPanel.tsx`, `src/renderer/styles.css`
  - 起動時に active session の messages を読み込むようにした。
  - History dialog を追加し、過去 session の選択、New Chat、Delete を扱えるようにした。
  - New Chat は現在の会話を消さず、新しい空 session を active にする動きにした。
  - 送信成功後、assistant / tool message まで含む transcript を保存するようにした。

- `docs/30_設計/86_AIChatPanelとAgentTooling.md`
  - AI Chat 履歴永続化を実装済み仕様として追記した。
  - 未実装一覧から multi-session 永続化を外した。

- 検証
  - `npm run build` 成功。
