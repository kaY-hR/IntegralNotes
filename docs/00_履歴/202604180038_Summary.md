# 2026-04-18 Summary

## 依頼

- Python 実行 block の output row で、slot 名と folder と file 名の間に改行が入っていて見た目が悪い
- slot 名、folder、file 名を 1 行に並べたい

## 対応

- `src/renderer/integralCodeBlockFeature.tsx` の output row markup を調整し、主要要素を `integral-output-slot-row__main` の 1 行レイアウトにまとめた
- slot 名と最新 dataset 表示を同じ slot 領域に寄せ、folder picker、dataset 名 textarea、`.idts` suffix、note button を同一行に並ぶ構造へ変更した
- `src/renderer/styles.css` の output row style を縦積み前提から横並び前提へ変更した
- 狭い幅では折り返せるように mobile 側だけ column へ落とす挙動は維持した

## 変更箇所

- `src/renderer/integralCodeBlockFeature.tsx`
- `src/renderer/styles.css`

## 検証

- `npm run build` が通った
