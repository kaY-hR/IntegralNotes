# 2026-04-30 10:26 doc-review

## 目的

`doc-review` スキルに基づき、`docs/10_要求` を起点として `docs/20_アーキテクチャ` / `docs/30_設計` と `src` 実装の整合性を確認した。

## 確認範囲

- 直近履歴: `202604301018.md`, `202604301016.md`, `202604300952.md`
- 要求: `docs/10_要求/*.md`
- アーキテクチャ: `docs/20_アーキテクチャ/*.md`
- 設計: `docs/30_設計/*.md`
- 実装: `src/shared/integral.ts`, `src/main/integralWorkspaceService.ts`, `src/main/workspaceService.ts`, `src/main/dataNote.ts`, `src/renderer/integralBlockRegistry.tsx`, `src/renderer/integralCodeBlockFeature.tsx`

## 主な指摘

- `未実装/追随不足 | 高`: Python block 実行時、`in:` の path は内部的に managed data ID へ解決されるが、成功後に返る block source は `sourceBlock` ベースのため、`inputs` が ID に書き戻されない。要求・設計の「保存または実行前に `in:` を ID へ正規化」とずれる。
- `矛盾 | 高`: managed file / generated file metadata の key が、要求・アーキテクチャでは `formatId`、設計例・実装では `format` になっている。
- `矛盾 | 中`: `.idts` manifest の key が、要求・データモデルでは `id / formatId / bundleRootPath` 系、ワークスペース設計・実装では `datasetId / kind / dataPath` 系で揺れている。
- `矛盾 | 中`: data-note の解決基準が、要求では `noteTargetId`、一部アーキテクチャ・リンク設計では `data ID` / `id` と書かれている。実装は `noteTargetId` を使っている。
- `要確認 | 中`: `.idts` manifest を「それ自体も managed file」とする文書と、dataset entity として扱う文書・実装が混在している。

## 確認

- レビューのみ。`npm run build` 等は未実行。
