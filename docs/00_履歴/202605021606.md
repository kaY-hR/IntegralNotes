# 2026-05-02 16:06 仕様文書追随レビュー

## 依頼

- `doc-review` スキルを使い、最近の実装に仕様文書が追随しているか確認した。
- source code を第一 truth、`docs/00_履歴` を第二 truth とし、`docs/10_要求` / `docs/20_アーキテクチャ` / `docs/30_設計` の乖離を見た。

## 確認した主な truth

- 直近履歴では Workspace Template と Inline Action registry が中心だった。
- 2026-04-30 の履歴では `.idts` output を visible output folder path にし、folder 内に manifest を作る方針へ変更済み。
- 現行実装でも `.idts` output は folder path を要求し、Python へ visible folder path を渡す。
- 現行実装では `.idts` file open / embed は dataset data-note を開く・表示する方向へ寄っている。
- `.inline-action` は通常は hidden だが、hidden 表示 ON の file tree では見えるように snapshot 除外から外されている。

## 主な指摘

- `.idts` output について、要求と実装は visible output folder だが、複数のアーキテクチャ/設計文書に hidden bundle directory や `.idts` manifest path 指定の旧記述が残っている。
- `.idts` 表示について、要求と実装は data-note 表示だが、`docs/30_設計/40_標準描画と結果閲覧.md` は dataset manifest viewer / DataSetViewer 前提の旧記述が残っている。
- `.inline-action` の file tree 表示について、履歴と実装は hidden 表示 ON なら見える挙動だが、Inline Action 設計は「通常の file tree には表示しない」までしか書いておらず、hidden toggle との関係が追記不足。
- AI Chat Settings Dialog は実装と履歴上、chat panel system prompt だけを編集し Inline Action prompt は専用 UI に分離しているが、AI Chat 設計文書側にこの整理の明記が薄い。

## 確認

- レビューのみ。build / test は実行していない。
