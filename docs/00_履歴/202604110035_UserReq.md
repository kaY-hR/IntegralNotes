## フィードバック

### ワークスペース

#### ワークスペース未設定時の導線を見直したい
優先重み:10

* ワークスペース未設定時に自動でフォルダ選択ダイアログを出す必要はない
* その代わり、エクスプローラーパネルは空状態にして、そこへ `フォルダを開く` ボタンを表示したい
* flex-layout 背景に出ている `Open first note` / `New note` の導線は不要
* ファイルが存在しないと成立しない操作が見えているため、未設定時や空ワークスペース時の表示はもっと素直にしたい
