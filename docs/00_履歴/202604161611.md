# 2026-04-16 16:11

- Python callable 用の正式 SDK を追加した。配置は `plugin-sdk/python/integral/__init__.py` で、`integral_block`, `get_integral_block_spec`, `IntegralBlockSpec` を提供する。
- Python SDK を standalone package として扱えるように `plugin-sdk/python/pyproject.toml` と `plugin-sdk/python/README.md` を追加した。
- Electron main 側の Python runner を変更し、旧 `sys.modules["integral"]` shim 差し込みをやめて、SDK root を `sys.path` へ追加する方式に切り替えた。
- SDK root は開発時 `plugin-sdk/python`、packaged app では `process.resourcesPath/python-sdk` を使う。必要なら `INTEGRALNOTES_PYTHON_SDK_ROOT` で上書きできるようにした。
- packaging 設定として `package.json` の `build.extraResources` に `plugin-sdk/python -> python-sdk` を追加し、配布物へ Python SDK を同梱するようにした。
- デモ用 Python callable として `scripts/demo_hello_report.py` を追加した。出力 dataset に `index.html`, `payload.json`, `README.txt` を生成する最小例になっている。
- 既存の `scripts/demo_dataset_report.py` はそのまま維持し、やや実用寄りのデモとして残した。
- Python 実行の仕組みをまとめる設計文書として `docs/30_設計/35_ElectronからPythonを呼ぶ仕組み.md` を新規追加した。
- 既存文書 `docs/20_アーキテクチャ/30_Python汎用解析プラグイン.md` と `docs/30_設計/30_Pythonスクリプト登録と実行.md` は、SDK 実装済みの状態に合わせて更新した。
- `npm run build` は成功し、Python SDK import テストと `scripts/demo_hello_report.py` の単体実行テストも成功した。
