# 2026-04-16 18:12

- ユーザーの監査指摘に基づき、canonical path 前提を撤回した現行方針へ仕様文書を更新した。
- `original data` は hidden/visible の別コピーを持たず、`id`・`path`・`hash` の 3 点セットで一意管理し、`path` または `hash` の片側変更時は metadata を追随更新する前提へ文書を揃えた。
- `data-note` は hidden かつ system-managed である点を維持しつつ、hidden 表示切替は explorer 表示上の都合にすぎないことを文書上でも明確化した。
- built-in viewer の仕様文書を現行実装に合わせ、`.htm`, `.bmp`, `.gif`, `.webp` と text 系拡張子群、未知拡張子の text fallback を反映した。
- hidden file が Milkdown の link/embed 補完候補へ混入しうる件は実装せず、`docs/Issues.md` に Issue 30 として起票した。
- レガシー互換の dataset schema 受理を `src/shared/integral.ts`, `src/main/dataNote.ts`, `src/main/integralWorkspaceService.ts` から削除し、`representation: "directory"` や `storeRelativePath` / `sourceMembers` など旧項目の受理をやめた。
- その整理で発生した未使用ヘルパと死んだ分岐も削除し、`npm run build` が通ることを確認した。
