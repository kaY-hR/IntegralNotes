# main 取り込み

## 作業

- `link-picker-relation-ranking` で link picker ranking 実装を先に commit。
  - `24d98d8 Add relation-aware link picker ranking`
- `origin/main` を fetch 済みの状態から merge。
  - 取り込み元: `origin/main` `28e1853`
  - merge commit: `35df2ad Merge remote-tracking branch 'origin/main' into link-picker-relation-ranking`

## 補足

- 最初の merge は sandbox ACL により `.codex/skills/implement-integral-block/SKILL.md` の作成で失敗。
- 失敗した merge の未追跡ファイルを cleanup し、権限付きで merge を再実行した。
- `App.tsx` / `MilkdownEditor.tsx` / `styles.css` は auto-merge され、手動 conflict 解消は不要だった。

## 確認

- `npm run build` 成功。
