# 2026-04-14 14:25

## 会話要約

- dataset に `name` を持たせたい、明示的に作る source dataset ではユーザーが名称を付けたい、という要望に対応した。
- data-note の file 名は、dataset では dataset 名、original data では元ファイル名を基本にし、重複時は連番で解消したい、という要望に対応した。
- 要件/設計文書もこの命名ルールに合わせて更新した。

## 実施したこと

- `src/shared/integral.ts` を更新し、dataset summary に `name` を追加し、source dataset 作成 request でも `name` を受け取れるようにした。
- `src/main/integralWorkspaceService.ts` を更新した。
  - dataset metadata に `name` を追加した
  - user 操作で作る source dataset では request の `name` を保存するようにした
  - system が自動生成する dataset では `name` に `datasetId` を使うようにした
  - dataset / original data summary の `dataNoteRelativePath` は、実際に同期された data-note file から引くようにした
- `src/main/dataNote.ts` を更新した。
  - dataset data-note frontmatter に `name` を追加した
  - original data / dataset の default title を人間向け名称ベースへ変更した
  - data-note file 名の採番ロジックと、frontmatter から note 対象を引く helper を追加した
- `src/main/workspaceService.ts` を更新した。
  - `data-catalog/` 全体を走査し、existing frontmatter を見ながら managed note の file 名を再割当するようにした
  - `originalName.md` / `datasetName.md` を基本とし、衝突時は `_<serial>.md` を付けるようにした
  - 旧命名の canonical note は新命名へ寄せて再配置するようにした
- `src/renderer/IntegralAssetDialogs.tsx` と `src/renderer/PythonScriptDialog.tsx` を更新した。
  - source dataset 作成時に dataset 名入力欄を追加した
  - Python script 登録時に、original data から自動で source dataset を作る場合も名称を持たせるようにした
  - dataset picker では dataset ID だけでなく dataset 名を主表示にした
- 文書を更新した。
  - `docs/10_要求/10_MVP要件.md`
  - `docs/10_要求/20_ユーザー体験.md`
  - `docs/20_アーキテクチャ/10_データモデル.md`
  - `docs/30_設計/20_ワークスペースレイアウト.md`
  - `docs/30_設計/30_Pythonスクリプト登録と実行.md`

## 確認結果

- `npm run build` は成功した。

## 補足

- data-note file 名の重複解消は `_<serial>` 形式とした。
- existing data-note があれば frontmatter と本文を引き継ぎつつ、file 名だけを新ルールへ寄せる。
