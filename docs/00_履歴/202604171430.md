# 2026-04-17 14:30

- `original-data` の自動登録と、行方不明 managed data の整理フローを実装した。
- 着手前に `docs/10_要求/00_プロダクト概要.md`, `10_MVP要件.md`, `20_ユーザー体験.md` と、直近履歴 `202604170824.md`, `202604170016.md`, `202604170030.md` を確認した。

- `src/main/integralWorkspaceService.ts`
  - 起動時や Sync 時に走る reconcile フローへ、`cwd` 配下の未管理ファイルを `ORD-*` として自動登録する処理を追加した。
  - 自動登録対象は `md` 以外の file とし、既存 managed path、managed directory 配下、tracking issue の候補 path、hidden / system 系ディレクトリ配下は除外した。
  - `path/hash` のどちらでも追跡できず候補 path も無い managed data について、tracking issue を `missing` として返すようにした。
  - `missing` issue を confirm 後に metadata から削除できるようにし、次回 sync で data-note も整理されるようにした。
  - 手動 import 時の original-data 登録も内部 helper 経由に寄せ、複数件登録時は data-note sync を最後に 1 回だけ行うようにした。

- `src/shared/integral.ts`
  - managed data tracking issue に `kind: "relink" | "missing"` を追加した。
  - tracking issue 解決 request を `action: "relink" | "remove"` + `selectedPath?` へ拡張した。

- `src/renderer/App.tsx`
  - 既存の tracked path 更新ダイアログは `relink` issue のときだけ表示するようにした。
  - `missing` issue のときは confirm ダイアログを出し、了承時に管理対象から外せるようにした。
  - Sync 後の status message を、`relink` と `remove` で分けて出すようにした。

- 検証
  - `npm run build` が通ることを確認した。
