# 2026-04-16 16:31

- `.idts` の `![](...)` embed を IntegralNotes 標準 embed wrapper から切り離し、既存 `DataSetViewer` を直接描画する経路へ変更した。
- これにより `.idts` embed では、標準 embed の click-to-open / resize / pointer-blocking を使わず、`DataSetViewer` 本来の操作を優先するようにした。
- `itg-notes` の `out:` は初期値 `auto` を維持しつつ、実行成功後は生成された hidden `.idts` path を保持できるように parser / serializer を更新した。
- Python block 実行成功時には、renderer が実行結果の block source を再シリアライズしてノート上の `out:` を書き戻すようにした。
- main process 側では、output slot ごとに作成された dataset の manifest path を canonical workspace target に変換し、実行結果 block の `outputs` に載せて返すようにした。
- 関連文書として `docs/10_要求/20_ユーザー体験.md`, `docs/20_アーキテクチャ/00_全体アーキテクチャ.md`, `docs/20_アーキテクチャ/10_データモデル.md`, `docs/20_アーキテクチャ/20_ブロックとプラグイン.md`, `docs/30_設計/10_ブロックJSON設計.md`, `docs/30_設計/30_Pythonスクリプト登録と実行.md`, `docs/30_設計/35_ElectronからPythonを呼ぶ仕組み.md`, `docs/30_設計/40_標準描画と結果閲覧.md` を最新仕様に更新した。
- `npm run build` は成功した。
