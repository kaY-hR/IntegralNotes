# 2026-05-02 17:50 高優先度仕様追随更新

## 依頼

- 直前の `doc-review` で `矛盾 | 高` とした 2 件について、実装と履歴を truth として仕様文書を更新した。

## 更新内容

1. 実行済み block の操作を、従来の「削除だけ可能」から `Delete / Undo` に更新した。
   - Undo は生成済み managed output の file / dataset folder、metadata、data-note、Markdown link / embed 参照を整理する。
   - Undo 後は同じ block 定義から新しい block ID / 初期 input / 初期 params / 新しい output path を持つ draft を作り直す。
   - 再実行 button は出さず、再実行したい場合は Undo で draft に戻すか、新しい block を作る方針にした。
2. data-note の保存 path / open key を `noteTargetId` 基準に統一した。
   - `.store/.integral/data-notes/{id}.md` や data ID 起点の旧記述を `{noteTargetId}.md` / `noteTargetId` 起点へ更新した。
   - `share_note_with_input` で note target を共有する現行実装と整合するようにした。

## 変更した文書

- `docs/10_要求/00_プロダクト概要.md`
- `docs/10_要求/10_MVP要件.md`
- `docs/10_要求/20_ユーザー体験.md`
- `docs/20_アーキテクチャ/00_全体アーキテクチャ.md`
- `docs/20_アーキテクチャ/10_データモデル.md`
- `docs/20_アーキテクチャ/20_ブロックとプラグイン.md`
- `docs/30_設計/10_ブロックJSON設計.md`
- `docs/30_設計/20_ワークスペースレイアウト.md`
- `docs/30_設計/30_Pythonスクリプト登録と実行.md`
- `docs/30_設計/35_ElectronからPythonを呼ぶ仕組み.md`
- `docs/30_設計/60_ノートリンク記法.md`

## 確認

- 旧表現検索: `削除だけ` / `削除のみ可能` / `{id}.md` / `data ID から` / `path ではなく data ID` などを確認し、高2件に関する残りを解消した。
- 文書更新のみのため build / test は実行していない。
