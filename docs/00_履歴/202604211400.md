# 2026-04-21 14:00 AI Chat: tool message / image attachment / OverlayFs再検証

## 実施内容

- `AI Chat` の message model を拡張し、`tool` role と `image attachment` を追加した
- renderer の chat transcript で、tool 実行を assistant message の trace details ではなく独立 message として表示するようにした
- composer に `Attach Images` を追加し、画像プレビュー付きで送信できるようにした
- main process 側で、prompt 内の画像パスを自動検出して attachment 化する処理を追加した
  - quoted path
  - Windows absolute path
  - workspace relative path
- 実保存用の `writeWorkspaceFile` tool を追加した
  - `bash/writeFile` は引き続き preview-only
  - `.md` 既存ファイルは `workspaceService.saveNote()` を通して frontmatter を保持する
  - その他 text file は UTF-8 で実保存する

## 変更ファイル

- `src/shared/aiChat.ts`
- `src/main/aiAgentService.ts`
- `src/main/aiChatService.ts`
- `src/renderer/AIChatPanel.tsx`
- `src/renderer/styles.css`

## 検証

- `npm run build` は成功
- `just-bash` / `bash-tool` を `npm install ...@latest` で確認したが、公開 latest は引き続き:
  - `just-bash 2.14.2`
  - `bash-tool 1.3.16`
- その latest で `OverlayFs` を Windows 上で再現確認した結果:
  - `pwd` は成功
  - `find . -maxdepth 2` は成功
  - `ls src` は `No such file or directory`
  - `find src -type f` は `No such file or directory`
  - `cat src/main/aiAgentService.ts` も失敗

## 判断

- 現時点の npm 公開版では Windows + `OverlayFs` はまだ本線に戻せない
- したがって、workspace 探索は当面 `file preload / in-memory snapshot` を維持する
- upstream の Windows fix が release されたら再評価する
