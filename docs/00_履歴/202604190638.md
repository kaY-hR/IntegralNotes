# 2026-04-19 06:38

## 概要

- 仕様文書の未更新箇所を、現在の実装方針（`share_note_with_input` + `embed_to_shared_note` + `noteTargetId`）に合わせて更新した。
- 旧 `project_to_inputs` 前提の記述を要求・設計・Issue から除去した。

## 更新した文書

- `docs/10_要求/00_プロダクト概要.md`
  - data-note の紐づけを `ID 1:1` から `noteTargetId` 解決へ更新。
- `docs/10_要求/10_MVP要件.md`
  - output slot 契約を `auto_insert_to_work_note` / `share_note_with_input` / `embed_to_shared_note` に更新。
  - object form 例および実行時挙動の説明を新契約へ更新。
- `docs/10_要求/20_ユーザー体験.md`
  - block 実行時の自動追記フローを新契約へ更新。
  - data-note open を `noteTargetId` 基準へ更新。
- `docs/30_設計/10_ブロックJSON設計.md`
  - note projection metadata 例を `share_note_with_input` + `embed_to_shared_note` へ更新。
- `docs/30_設計/30_Pythonスクリプト登録と実行.md`
  - slot metadata 一覧、例、挙動説明を新契約へ更新。
- `docs/Issues.md`
  - Issue 44 の契約説明を新キーへ更新。

## 確認

- `npm run build` 成功
