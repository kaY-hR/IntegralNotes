# 2026-04-17 00:30

- Search sidebar の UI を VS Code 風の配置へ寄せて調整した。
- `src/renderer/SearchSidebarView.tsx` を組み替え、次を変更した。
  - header の説明文と大きい summary box を削除した
  - 上段を `検索` title + compact icon button 群へ変更した
  - query input の右に `Aa / ab / *` option button を並べる配置へ変更した
  - replace / include / exclude は query の下へ積む構成にした
- `src/renderer/styles.css` を更新し、Search pane 全体を `grid` ではなく `flex column` に変更した。
  - これにより、replace と file filter を同時に開いても結果 list に重ならないようにした
  - result card まわりの padding / border / line density も VS Code 風に寄せて軽量化した
- `npm run build` が通ることを確認した。
