# 2026-04-28 12:35 会話まとめ

## 目的

AI Chat の LLM が Markdown 内の HTML link / embed を見つけたとき、HTML を視覚レンダリングして確認する tool を使いやすくする。

## 実施内容

- 既存の AI Chat system prompt 相当は `ToolLoopAgent` の `instructions` として `src/main/aiAgentService.ts` で組み立てていることを確認した。
- `renderWorkspaceDocument` は既に Electron の hidden `BrowserWindow` と `webContents.capturePage()` で HTML / Markdown / Text を PNG 化し、tool result の `image-data` として LLM に返す実装になっていることを確認した。
- agent instructions に、Markdown 内で linked / embedded `.html` / `.htm` を見つけ、視覚内容が重要な場合は Markdown source だけで判断せず、その HTML path に対して `renderWorkspaceDocument` を呼ぶよう追記した。
- bash tool 側の追加 instructions にも同じ方針を追記した。

## 確認

- ユーザー指示により、build / test は実行していない。
