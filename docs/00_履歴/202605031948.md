# 2026-05-03 19:48 Inline Action 挿入直後の block view refresh 修正

## 依頼

- @@continue / @@mkpy の挿入直後はまだ default block のままだが、カーソルを移動すると Python block 表示へ変わる。

## 確認された状況

- 
pm run build は成功。
- 挿入された Markdown 自体は概ね正しく、追加の editor transaction が発生すると itg-notes block view に差し替わる挙動。

## 対応

- src/renderer/MilkdownEditor.tsx の Inline Action 挿入処理で、insert(markdown) 後に次フレームで refresh 用 transaction を dispatch するようにした。
- カーソル移動で直っていた view 更新を、挿入直後に自動で発生させる狙い。

## 備考

- 
pm run dev ログに出ていた editor destroy エラーと asset catalog JSON parse エラーは、今回の block view refresh とは別件として未対応。
- 追加修正後の build は未実行。
