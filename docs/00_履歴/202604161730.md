# 2026-04-16 17:30

- ユーザー指摘により、問題は app 実行時ではなく VS Code で `cwd` を開いたときの Python import 解決だと整理した。
- 原因は、`integral` package は `cwd/scripts/integral/` にある一方で、`demo_dataset_report.py` のような script は `cwd/` 直下にも置かれるため、Pylance/Pyright が `./scripts` を import path に含めていなかったこと。
- `src/main/integralWorkspaceService.ts` を更新し、workspace 初期化時に `cwd/.vscode/settings.json` をマージ更新して `python.analysis.extraPaths = ["./scripts"]` と `python.autoComplete.extraPaths = ["./scripts"]` を補助設定するようにした。
- 既存 setting が object の場合は維持しつつ、配列へ `./scripts` を重複なく追加する実装にした。
- 現在の repo root 用 `.vscode/settings.json` と、実ワークスペース例 `Notes/.vscode/settings.json` にも同じ設定を追加し、今すぐ VS Code 側で解決できる状態にした。
- 設計文書 `docs/30_設計/30_Pythonスクリプト登録と実行.md` と `docs/30_設計/35_ElectronからPythonを呼ぶ仕組み.md` に、authoring 補助として `.vscode/settings.json` を補助設定してよいことを追記した。
- `npm run build` は成功した。
