# 2026-04-21 11:45

## 概要

- Activity Bar から開ける `AI Chat` tab の最小実装を追加した
- AI Gateway 用の API key / model 設定導線と、モデル一覧取得の基礎を追加した

## 今回の変更

- `src/renderer/workspaceToolPlugins.tsx`
  - `builtin:ai-chat` を追加した
  - Activity Bar に chat icon を出し、既存の workspace-tool plugin 経路で main workspace tab を開くようにした
- `src/renderer/App.tsx`
  - AI Chat tab へ `workspace root name` と `selected paths` を渡すようにした
- `src/renderer/AIChatPanel.tsx`
  - chat panel UI を追加した
  - message list / composer / clear を追加した
  - active file / note の excerpt と selected paths を文脈表示するようにした
  - `Vercel AI Gateway` 固定の connection section を追加した
  - API key 入力、model select、`Refresh Models`、`Save Settings`、`Clear Key` を追加した
- `src/main/aiChatService.ts`
  - AI Chat 用 main process service を追加した
  - `userData/ai-chat-settings.json` へ設定を保存するようにした
  - API key は `safeStorage` が使える環境では暗号化保存するようにした
  - `https://ai-gateway.vercel.sh/v1/models` から model catalog を取得し、失敗時は fallback model list に落とすようにした
  - 応答本体はまだ stub で、renderer -> preload -> main process の経路確認用にしている
- `src/shared/aiChat.ts`, `src/shared/workspace.ts`, `src/main/preload.ts`, `src/main/main.ts`
  - AI Chat 用の shared type / IPC / preload API を追加した
- `src/renderer/styles.css`
  - AI Chat panel 用の layout / settings / message bubble / composer style を追加した

## 確認

- `npm run build` 成功

## 補足

- 現時点で `AI Chat` は UI と設定導線、model catalog 取得、stub 応答まで
- Vercel AI SDK 実接続、tool 実行、skills、MCP 実行は次段
