# 2026-04-27 22:35

- Open Folder で別 cwd を開いた後も、renderer 側の Integral runtime cache が残り、`>` の Python callable 候補が起動時 workspace のままになり得る問題を修正した。

- `src/renderer/integralPluginRuntime.ts`
  - asset catalog の one-shot cache を外部から再適用できる `setIntegralPluginRuntimeCatalog()` を追加した。
  - reset / set / async load の競合で古い catalog が後勝ちしないよう、load version を導入した。

- `src/renderer/App.tsx`
  - workspace runtime invalidation を共通化し、Open Folder と明示 Sync で `resetIntegralPluginRuntime()` と revision 更新を行うようにした。
  - Sync 時は新設の `window.integralNotes.syncWorkspace()` を呼び、workspace snapshot / asset catalog / tracking issues を再読込するようにした。
  - asset catalog refresh 時に renderer runtime cache も同じ catalog へ更新するようにした。
  - plugin install / uninstall / refresh 後も asset catalog を再読込するようにした。

- `src/main/workspaceService.ts`, `src/main/main.ts`, `src/main/preload.ts`, `src/shared/workspace.ts`
  - `workspace:sync` IPC と `syncWorkspace()` API を追加した。
  - `setRootPath()` と明示 Sync では skill bootstrap を force 実行し、`Notes/.codex/skills` / `Notes/.claude/skills` を cwd 側へ再同期するようにした。

- `src/renderer/AIChatPanel.tsx`, `src/renderer/workspaceToolPlugins.tsx`
  - workspace revision を受け取り、Sync / runtime reload 後に AI Chat status を読み直すようにした。
  - AI Chat の skill directory 表示と runtime auth 状態が古い cwd のまま残りにくくした。

- `src/renderer/ProcessChainViewer.tsx`
  - workspace revision 変更時に note cache を破棄し、Sync 後の process chain が古い note 内容を使い続けないようにした。

- `src/renderer/integralCodeBlockFeature.tsx`
  - Python block の file input / output directory 候補で、node view 作成時の `workspaceEntries` 配列を固定保持しないようにした。
  - picker render 時に最新の workspace entries を getter 経由で読むことで、Sync や workspace 更新後の候補が古くならないようにした。

- dataclass を使う Python script が落ちる原因を確認し、修正した。
  - 原因は Python 3.10 ではなく、runner が `importlib.util.module_from_spec()` で作った module を `sys.modules` に登録しないまま `exec_module()` していたこと。
  - `@dataclass` は `cls.__module__` から `sys.modules[...]` の module dict を参照するため、未登録だと `AttributeError: 'NoneType' object has no attribute '__dict__'` になる。
  - `src/main/integralWorkspaceService.ts` の runner で `sys.modules[spec.name] = loaded_module` を `exec_module()` 前に行うようにした。
  - script の sibling import も動きやすいよう、script directory を `sys.path` に追加した。

- 検証
  - `npm run build` 成功。
  - dataclass の最小再現で、修正前条件では `AttributeError: 'NoneType' object has no attribute '__dict__'` が出ることを確認。
  - `sys.modules` 登録後は同じ dataclass script が `Row(value=1)` を返すことを確認。
