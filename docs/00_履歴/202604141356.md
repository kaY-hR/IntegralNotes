# 2026-04-14 13:56

## 会話要約

- `Issue 14` として、dataset / original data の ID prefix を `DTS-...` / `ORD-...` に切り替えたい要望に対応した。
- `Issue 15` として、dataset 作成時も `data-catalog/` に data-note を自動生成したい要望に対応した。
- 併せて、data-note 実装を original data 専用から汎用化し、source / derived dataset の両方で同じ frontmatter / 本文保持ルールを使うようにした。

## 実施したこと

- `src/main/dataNote.ts` を追加し、data-note ユーティリティを original data / dataset 両対応へ汎用化した。
  - original data note / dataset note の frontmatter 生成
  - data-note 本文の抽出と保存時の本文差し替え
  - metadata JSON から note 対象を判定する type guard
- `src/main/workspaceService.ts` を更新した。
  - `syncDataCatalogNotes()` に改め、`.store/.integral/*.json` から original data note と dataset note の両方を同期するようにした
  - `data-catalog` 配下で frontmatter を隠し、本文だけを editor / viewer に渡す既存経路を汎用 data-note 判定へ切り替えた
- `src/main/integralWorkspaceService.ts` を更新した。
  - new ID 生成を `ORD-...` / `DTS-...` に切り替えた
  - original data import 時は `ORD-...` を発行するよう変更
  - source dataset 作成時と Python block 実行時の output dataset 作成時に、対応する dataset data-note を即時生成するよう追加
  - `IntegralDatasetSummary` / `IntegralDatasetInspection` 相当の返却値に `dataNoteRelativePath` を載せた
- `src/shared/integral.ts` を更新し、dataset summary に `dataNoteRelativePath` を追加した。
- 文書を更新した。
  - `docs/10_要求`, `docs/20_アーキテクチャ`, `docs/30_設計` の current docs を `ORD-...` / `DTS-...` 前提へ更新
  - dataset data-note を source / derived とも自動生成する方針へ更新
  - `docs/Issues.md` で `Issue 14`, `Issue 15` を completed に更新

## 確認結果

- `npm run build` は成功した。

## 補足

- 未リリース前提として、明示的な migration は追加していない。
- ただし metadata 読み込み自体は string ベースのため、既存開発用 workspace の読み取りを即座に壊す方向にはしていない。
