# 2026-04-19 09:06

## 概要

- process chain viewer の探索範囲を、full reachable graph ではなく直系 ancestor / descendant のみへ絞った
- edge の接続点を `in = left`, `out = right` の fixed port 表示に変更した
- 関連する要求文書、設計文書、Issue を新しい挙動へ更新した

## 実装

- `src/renderer/ProcessChainViewer.tsx`
  - graph 探索を方向付きに変更した
  - root block から upstream には direct parent lineage、downstream には direct child lineage だけを辿るようにした
  - ancestor 側の別 child branch や descendant 側の別 parent branch を拾わないようにした
  - edge 接続点を node 左右の fixed port 前提へ変更した
- `src/renderer/styles.css`
  - process chain viewer node の left / right port 表示を追加した

## 文書

- `docs/10_要求/20_ユーザー体験.md`
  - process chain viewer が直系 ancestor / descendant のみを描画することを追記
  - `in = left`, `out = right` の固定 port を追記
- `docs/30_設計/80_ActivityBarとSidebarView.md`
  - process chain viewer section に lineage 制約と fixed port を追記
- `docs/30_設計/85_ProcessChainViewer.md`
  - graph 探索ルールを full reachable graph から direct lineage へ更新
  - edge / port 仕様を追加
- `docs/Issues.md`
  - Issue 38 に 2026-04-19 の調整内容を追記

## 確認

- `npm run build` 成功
