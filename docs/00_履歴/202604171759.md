# 2026-04-17 17:59

- LabSolutions の LC text export を読み取り、1 txt = 1 chromatogram として 1 つの JSON に集約する Python block を追加した。
  - `Notes/src/lc_text_to_chromatogram_json.py`
  - 入力 dataset directory 内の `*.txt` を走査し、`[LC Chromatogram(...)]` セクションを持つ file だけを対象にする。
  - 出力 JSON 形式は `{"filename": {"rt": [...], "intesity": [...]}}`。
  - `Output2.txt` のようにクロマトグラム section が無い file は無視する。

- 上記 JSON を 1 つだけ含む dataset を入力に取り、クロマトグラムを重ね描きした Plotly HTML を出力する Python block を追加した。
  - `Notes/src/chromatogram_json_to_plotly_html.py`
  - 入力 dataset 内の JSON file がちょうど 1 つであることを検証する。
  - JSON 内の各系列を重ね描きし、`index.html` を出力する。
  - `plotly` package が利用可能なら inline JS 付き HTML、無ければ CDN fallback HTML を出す。
  - `dataclass` は使っていない。

- 確認
  - `py_compile` で両 script の構文確認を実施。
  - `.store/objects/DTS-73A62694/chromatograms.json` を入力にして `chromatogram_json_to_plotly_html.py` を直接呼び、`index.html` 出力を確認した。
