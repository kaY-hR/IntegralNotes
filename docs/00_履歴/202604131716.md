# 2026-04-13 17:16

## 会話要約

- `shimadzu-lc migration` を進める依頼があった。
- 途中で別件キューとして、main app のエクスプローラー/ノート表示でも `chunk viewer` 相当の表示を入れたい要望が追加された。

## 実施内容

- `docs/Issues.md`
  - `## 7. エクスプローラー / ノート表示を chunk viewer 相当に広げたい` を追加。
- external plugin を新しい integral block catalog に載せる実装を追加。
  - `src/shared/integral.ts`
    - external plugin block 用メタデータを追加。
  - `src/main/integralWorkspaceService.ts`
    - installed plugin を asset catalog に統合。
    - `shimadzu.lc / LC.Method.Gradient` を `shimadzu-lc / run-sequence` として alias 公開。
    - `standard-graphs` は integral block catalog から除外。
    - external plugin block の generic fallback 実行を追加。
  - `src/main/main.ts`
    - `IntegralWorkspaceService` に `PluginRegistry` を注入。
- `shimadzu-lc` renderer bridge を追加。
  - `src/renderer/ExternalPluginBlockRenderer.tsx`
    - installed external plugin renderer を iframe で読み込み。
    - `params` 更新と plugin action 実行を `postMessage` で仲介。
  - `src/renderer/integralCodeBlockFeature.tsx`
    - external plugin block の場合は generic run bar の代わりに custom renderer を表示。
    - slot 割当 UI は共通 schema のまま維持。
  - `src/renderer/styles.css`
    - plugin renderer 用 iframe 表示サイズを調整。

## 確認

- `npm run build`
  - 成功

## 補足

- この対応で、旧 `shimadzu.lc` artifact が入っていても、ノート上の block JSON は `plugin: "shimadzu-lc"`, `block-type: "run-sequence"` の新設計寄りで扱えるようにした。
- plugin package source 自体の manifest 名称変更まではまだしていない。現状は app 側で alias 吸収している。
