# Summary

- Python 実行 block の output 名入力欄について、枠線が薄すぎて見えないという指摘に対応した。
- `src/renderer/styles.css` の `.integral-output-slot-row__name-input` を調整し、`0.5px solid rgb(60, 60, 60)` の枠線と白背景に変更した。
- 装飾的な `box-shadow` は除去し、hover / focus 時はより濃い `rgb(40, 40, 40)` の枠線になるようにした。
- `npm run build` を実行し、ビルド成功を確認した。
