# 2026-04-11 00:07

## 背景

- `scripts\copy-git-files.bat` 実行時に、`docs` 配下や `plugin-sdk/docs`、`src/renderer/resources` の日本語パスを含むファイルで `Skipped non-file tracked path` 警告が大量に出た。
- 実際には `docs` フォルダ自体が除外されていたのではなく、`git ls-files` の取得結果が文字化けし、`Test-Path` に失敗していた。

## 実施内容

- `scripts/export-tracked-files.ps1`
  - `git` 呼び出しを `Get-GitTrackedFiles` 関数へ切り出した。
  - `git -c safe.directory=* -c core.quotepath=false ls-files -z` を `System.Diagnostics.Process` 経由で実行し、標準出力を UTF-8 バイト列として明示的に読むように変更した。
  - `-z` 区切りを PowerShell 側で分解し、空要素を除外するようにした。
- `scripts/copy-git-files.bat`
  - 固定パス運用に合わせて `%*` の引数受け渡しを削除した。

## 検証

- sandbox 向け一時実行
  - `C:\Users\shimadzu\.codex\memories\copy-export-test` へ出力して `Copied: 72`, `Excluded: 72`, `Removed stale files: 0` を確認
  - `docs/20_アーキテクチャ/jsonスキーマ.md`
  - `docs/20_アーキテクチャ/pluginシステム.md`
  - `docs/10_要求/機能詳細イメージ/スライド4.PNG`
  - `plugin-sdk/docs/開発者向けアナウンス.md`
  - `src/renderer/resources/ファイル追加.png`
  - が存在することを確認
- 実共有先
  - `C:\Users\shimadzu\OneDrive - SHIMADZU\共有\test` へ再実行し、`Copied: 72`, `Excluded: 72`, `Removed stale files: 0` を確認
  - 共有先でも上記の日本語パスを含むファイルが存在することを確認
