# 2026-04-14 10:07

## 会話要約

- 一度は non-Markdown text editor を入れたが、触ってみた結果 `json` などをアプリ内で直接編集できる必要は薄い、という判断になった。
- 最終的に「`md` 以外は preview-only に戻す」で合意した。
- `git revert` を使うより、今回の差分だけを手で戻す方が安全、という方針でロールバックした。

## 実施内容

- non-Markdown text 編集の追加差分を取り下げた。
  - `src/shared/workspace.ts`
    - `saveWorkspaceTextFile()` を削除。
    - `WorkspaceFileViewKind` を `markdown / html / image / text / unsupported` に戻した。
  - `src/main/main.ts`
  - `src/main/preload.ts`
    - `workspace:saveTextFile` IPC を削除した。
  - `src/main/workspaceService.ts`
    - `.svg` を再び `image` 扱いに戻した。
    - `html` は read 時に `<base>` 注入した preview 用文字列を返す実装へ戻した。
  - `src/renderer/App.tsx`
    - non-Markdown text editor 分岐を削除し、`Ctrl+S` は再び Markdown tab のみ対象に戻した。
  - `src/renderer/WorkspaceTextEditor.tsx`
    - 削除。
  - `src/renderer/WorkspaceFileViewer.tsx`
    - `html / image / text / unsupported` の preview-only viewer に戻した。
  - `src/renderer/styles.css`
    - text editor 用スタイルを削除した。
- `docs/Issues.md`
  - task 8 は「今回は採用しない」扱いに更新した。

## 確認

- `npm run build`
  - これから確認
