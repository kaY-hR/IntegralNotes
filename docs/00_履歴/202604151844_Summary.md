# 2026-04-15 18:44

## 会話要約

- ユーザーから、「canonical 管理は廃止したので、original data 登録時に `.store/objects` へコピーや link を作るのはおかしい」と指摘があった。
- あわせて、data-note や source dataset materialize も hidden object ではなく visible path を正として参照すべき、という方針を再確認した。

## 実装内容

- `src/main/integralWorkspaceService.ts`
  - original data import から `.store/objects` canonical storage を外した。
  - `cwd` 外から登録した original data は visible な `Data/` 配下へ copy し、`cwd` 内の対象はその場の path をそのまま managed `path` として使うようにした。
  - new metadata では `objectPath` を書かないようにした。
  - `resolveOriginalDataContentPath()` を visible `metadata.path` 基点へ変更し、source dataset の materialize でも current visible path を参照するようにした。
  - reconciliation / tracked path 更新では `objectPath` を落とすようにし、旧 canonical 参照が段階的に消えるようにした。
- `docs/10_要求/10_MVP要件.md`
  - original data の canonical 実体を `.store/objects` に隠す構成は採らない、と明記した。
- `docs/20_アーキテクチャ/10_データモデル.md`
  - original data の current path は visible workspace path を基本とすること、source dataset materialize や data-note metadata は visible path を正にすることを追記した。
- `docs/Issues.md`
  - `Issue 24` に `実装第5段` として今回の修正を追記した。

## 検証

- `npm run build` が通ることを確認した。

## 補足

- derived dataset の hidden 配置自体は今回まだ維持している。
- `source / derived` の representation をどこまで揃えるか、manifest に補助 path を持たせるかは今後の設計論点として残っている。
