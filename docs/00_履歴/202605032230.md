# 2026-05-03 22:30 AI機能の AGENTS.md 確認指示追加

## 依頼

- 全AI機能について、`AGENTS.md` があれば読むことを system prompt に入れたい。
- 編集対象ファイルのあるフォルダ、およびその ancestor-line のフォルダに `AGENTS.md` がある場合、それらも確認するようにしたい。

## 対応

- `src/main/aiAgentService.ts` に共通の `AGENTS.md` 確認指示を追加した。
- 通常 AI Chat 用の `buildAgentInstructions` と、Inline AI / Promptless / Inline Action など task 実行用の `buildTaskInstructions` の両方へ同じ指示を合成するようにした。
- 既定 prompt だけではなく、ユーザーが保存済み system prompt や custom Inline Action prompt を使っている場合にも適用される。

## 確認

- `npm run build` 成功。
