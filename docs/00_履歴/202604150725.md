# 2026-04-15 07:25

## 会話要約

- `Issue 23` 実装後、ノート内画像が `Not allowed to load local resource: file:///...` で表示できない不具合報告があった。

## 実施したこと

- `src/main/workspaceService.ts` を確認し、`resolveWorkspaceFileUrl()` が editor 再表示用に `file:` URL を返していたことを確認した。
- Electron renderer 側で local resource 制約に引っかかるため、同メソッドが workspace 画像を読み込んで `data:` URL を返すように修正した。
- `docs/Issues.md` の `Issue 23` 実装メモを、`file:` URL 前提ではなく DOM 用 loadable URL / `data:` URL ベースの現状に合わせて更新した。

## 検証

- `npm run build` が通ることを確認した。

## 補足

- Markdown source に保存される canonical target は引き続き `![](/Data/...)` のままで、修正対象は editor 上の再表示経路のみ。
