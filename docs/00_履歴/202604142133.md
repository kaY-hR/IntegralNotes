# 2026-04-14 21:33

## 会話要約

- `Issue 23` として、Milkdown の画像挿入 / 画像貼り付けで `blob:` URL ではなく workspace 永続画像を保存したい依頼に対応した。

## 実施したこと

- `docs/00_履歴` の直近 3 件、`docs/Issues.md`、`docs/10_要求/10_MVP要件.md`、`docs/30_設計/70_ノート画像添付.md` を確認し、Issue 23 の要求を整理した。
- `node_modules/@milkdown` の型と実装を確認し、`Crepe.Feature.ImageBlock` に `onUpload` と `proxyDomURL` を与えれば image insert / paste / drop を共通 hook へ寄せられることを確認した。
- `src/shared/workspace.ts` を修正した。
  - note image 保存用の request / result 型を追加した。
  - renderer から呼ぶ `saveNoteImage` / `resolveWorkspaceFileUrl` API を追加した。
- `src/main/preload.ts` と `src/main/main.ts` を修正した。
  - note image 保存と workspace file URL 解決の IPC を追加した。
- `src/main/workspaceService.ts` を修正した。
  - `saveNoteImage()` を追加し、`Data/yyyyMMdd-HHmm-RRR.ext` 形式で画像を永続保存するようにした。
  - 拡張子は元 file 名または mime type から決め、判定不能時は `.png` を fallback にした。
  - Markdown に保存する canonical target は `![](/Data/...)` を返すようにした。
  - editor 再表示用に workspace relative path を `file:` URL へ変換する `resolveWorkspaceFileUrl()` を追加した。
- `src/renderer/MilkdownEditor.tsx` を修正した。
  - `Crepe.Feature.ImageBlock` に `onUpload` を設定し、upload 時に image bytes を main process へ送って永続化するようにした。
  - `proxyDomURL` を設定し、`/Data/...` など workspace path を DOM 表示用 `file:` URL へ変換するようにした。
  - upload 成功後に最新 workspace snapshot を App 側へ返すようにした。
- `src/renderer/App.tsx` を修正した。
  - note image 保存後に workspace snapshot を反映し、`Data/` 配下の新規画像 file が explorer に現れるようにした。
- `docs/Issues.md` の `Issue 23` を completed に更新し、実装内容を追記した。

## 検証

- `npm run build` を実行し、renderer / main ともに build が通ることを確認した。

## 補足

- 現在の実装では、Milkdown の image insert UI、paste、drop は同じ `onUpload` policy を通り、保存直後から Markdown source には `blob:` ではなく workspace canonical path が残る。
- 画像の explorer 貼り付けや保存先設定 UI は引き続き対象外で、Issue 23 のスコープ外として維持している。
