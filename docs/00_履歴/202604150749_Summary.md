# 2026-04-15 07:49

## 会話要約

- Markdown editor mode の user-facing 文言として `WYSIWYG` は一般ユーザーに伝わりにくい、という指摘を受けた。
- 代替として、機能名ベースの `Markdown編集` / `見たまま編集` に寄せる方針にした。

## 実施したこと

- `src/renderer/App.tsx` の mode toggle button 文言を更新した。
  - `テキスト編集` / `WYSIWYG に戻す` から `Markdown編集` / `見たまま編集` に変更した。
  - mode 切替時の status message も同じ語彙に揃えた。
  - tooltip も `Markdown テキストとして編集` / `見たまま編集に戻す` に更新した。
- `docs/10_要求/10_MVP要件.md` と `docs/20_アーキテクチャ/00_全体アーキテクチャ.md` の表現を現行 UI に合わせて更新した。

## 検証

- `npm run build` が通ることを確認した。

## 補足

- `WYSIWYG` は内部実装の説明には残してよいが、画面ラベルとしては jargon が強いため避けた。
- `rich view` は read-only preview に見えやすいため、editable mode 名としては採用していない。
