# 2026-04-10 08:05

## 背景

- `shimadzu-lc` plugin について、詳細モードとは別に `gradient.png` を使うシンプルモードを選べるようにしたいという追加フィードバックがあった。
- あわせて、`start B.Conc / peak B.Conc / final B.Conc` のようなサマリー表示は不要との指摘があった。
- 変更後の plugin 配布 zip も更新したいという流れになった。

## 実施内容

- `plugins/shimadzu-lc/renderer/index.html`
  - `詳細` / `シンプル` を切り替える mode switch を追加。
  - `シンプル` は `gradient.png` を使う画像ベース表示にした。
  - `詳細` は `analysis-time` と `time-prog` の編集を維持しつつ、不要な `start / peak / final B.Conc` サマリーカードは削除した。
  - モード切替は renderer 内のローカル表示状態として扱い、block JSON には保存しない構成にした。
- `plugins/shimadzu-lc/README.md`
  - `詳細` / `シンプル` モードの説明を追記した。
- `plugins/dist/shimadzu.lc/`
  - `npm run plugins:package:shimadzu-lc` を再実行し、zip と install/uninstall bat を再生成した。

## 検証

- `plugins/shimadzu-lc/renderer/index.html`
  - `<script>` 部分を `new Function(...)` でパース確認し、syntax error がないことを確認
- `plugins/dist/shimadzu.lc/shimadzu.lc-0.1.0.zip`
  - zip 更新時刻が `2026-04-10 08:05:23` になっていることを確認
  - zip 内に `gradient.png`, `renderer/index.html`, `host/index.cjs` が入っていることを確認
