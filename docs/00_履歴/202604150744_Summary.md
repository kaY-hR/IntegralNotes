# 2026-04-15 07:44

## 会話要約

- `Issue 3` として、Markdown tab の右上 button から本文をテキストとして直編集できるようにしたい依頼に対応した。
- 条件として、frontmatter は引き続き editor から分離し、本文だけを編集対象にする必要があった。

## 実施したこと

- `docs/00_履歴` の直近 3 件、`docs/Issues.md`、`docs/10_要求` を確認し、Issue 3 の既存メモと現在の frontmatter 契約を整理した。
- `src/renderer/App.tsx` を修正した。
  - markdown tab state に `editorMode: "wysiwyg" | "text"` を追加した。
  - tab content 右上に mode toggle button を追加した。
  - `WYSIWYG` と `body-only text` が同じ `content` / `savedContent` / dirty 判定 / `Ctrl+S` 保存導線を共有するようにした。
- `src/renderer/RawMarkdownEditor.tsx` を追加した。
  - 本文だけを monospaced textarea で編集できる plain text editor を実装した。
  - active tab での focus、textarea の自動高さ調整、Tab 入力を扱うようにした。
- `src/renderer/MilkdownEditor.tsx` を修正した。
  - 共通 toolbar slot を追加し、WYSIWYG 側でも同じ位置に mode toggle を表示できるようにした。
- `src/renderer/styles.css` を修正した。
  - editor toolbar と raw text editor のレイアウト / 見た目を追加し、既存の note paper 風 UI に揃えた。
- 文書を更新した。
  - `docs/Issues.md` の `Issue 3` を completed に更新し、実装内容を追記した。
  - `docs/10_要求/10_MVP要件.md` に Markdown tab の mode 切替要件を追記した。
  - `docs/20_アーキテクチャ/00_全体アーキテクチャ.md` の note layer を現状仕様に合わせて更新した。

## 検証

- `npm run build` が通ることを確認した。

## 補足

- raw text mode は full file 編集ではなく、frontmatter を除いた本文だけを扱う。
- frontmatter 保持、Markdown link / image rewrite、保存契約は既存の main process 実装をそのまま利用している。
