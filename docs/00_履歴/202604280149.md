# 2026-04-28 01:49

- Markdown editor の inline AI 呼び出しを追加した。
  - `??` を入力すると、トリガー文字を消費して AI 向けメッセージ入力 popup を開く。
  - 送信時は「その位置に挿入されるベストなテキストだけを返す」専用 system prompt で LLM を呼び、応答をカーソル位置へ挿入する。

- Markdown editor の Python block 自動生成を追加した。
  - `>>` を入力すると、AI 向けメッセージ入力 popup を開く。
  - 送信時は `implement-integral-block` skill 相当の prompt を使い、`writeWorkspaceFile` tool で Python script を実保存させる。
  - AI 応答から `scriptPath` / `functionName` を取り出し、保存済み script に `@integral_block` 直下の callable があることを確認してから `run: script.py:function` の `itg-notes` block を挿入する。
  - script 生成後に workspace sync と asset catalog refresh を行い、新規 callable の slot 定義を使える場合はそれに基づく block source を挿入する。

- `src/shared/aiChat.ts`, `src/shared/workspace.ts`, `src/main/main.ts`, `src/main/preload.ts`
  - inline AI 挿入と inline Python block 生成用の API / IPC を追加した。

- `src/main/aiAgentService.ts`, `src/main/aiChatService.ts`
  - AI Chat の runtime 選択を流用しつつ、chat transcript とは別の単発 task 実行を追加した。
  - `??` は tool なしの短い生成、`>>` は workspace tools / skill prompt 付き生成として分離した。

- `src/renderer/MilkdownEditor.tsx`, `src/renderer/styles.css`
  - `??` / `>>` trigger 検出、popup UI、送信中/error state、挿入処理を追加した。

- 検証
  - `npm run build` 成功。
  - `git diff --check` 成功。
