# 2026-04-17 16:10

- managed data 削除時の confirm の見え方を補強した。

- `src/renderer/App.tsx`
  - Explorer の削除ダイアログを開く際、削除対象 path に紐づく managed data を抽出する helper を追加した。
  - 対象に managed data が含まれる場合は、削除確認文言へ
    `metadata / data-note も削除し、必要なら source dataset の .idts も更新する`
    旨を追記するようにした。
  - これにより、managed cleanup が伴う delete でも、削除前にユーザーがその副作用を確認できるようにした。

- 検証
  - `npm run build` が通ることを確認した。
