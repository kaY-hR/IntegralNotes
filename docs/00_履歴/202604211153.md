# 2026-04-21 11:53

## 概要

- `AI Chat` tab の設定 UI を dialog へ移し、chat 本文領域を広くした
- `AI Gateway` の chat completions endpoint へ直接送信する最小実装を追加した

## 今回の変更

- `src/renderer/AIChatPanel.tsx`
  - header 右側に settings icon button を追加した
  - `Gateway Settings` を inline section ではなく dialog として開くように変更した
  - local dialog open 中は `integral-dialog-open` を付け、既存 modal と同じ扱いにした
  - API key / model / refresh / save / clear は dialog 側へ移した
- `src/renderer/styles.css`
  - AI Chat settings dialog 用の style を追加した
  - header actions と settings icon button の style を追加した
- `src/main/aiChatService.ts`
  - API key 設定済み時は `https://ai-gateway.vercel.sh/v1/chat/completions` へ送信するようにした
  - system prompt に workspace / active file / excerpt / selected paths を渡すようにした
  - API key 未設定時だけ stub 応答へ落とすようにした
  - status の `implementationMode` を `stub | gateway` に拡張した
- `src/shared/aiChat.ts`
  - runtime mode 型を更新した

## 確認

- `npm run build` 成功

## 補足

- 現時点で model catalog 取得と通常 chat 送信は `AI Gateway` に直接接続する
- まだ未実装なのは tool 実行、skills、MCP 実行、streaming 応答
