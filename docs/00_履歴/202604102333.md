# 2026-04-10 23:33

## 背景

- export script のコピー先パスは引数化ではなく、ファイル内の 1 か所を書き換えるだけで使い回せる方がよい、という方針が共有された。
- あわせて、直前の試行は user 側で revert 済みで、そこから最小差分で整理し直すことになった。

## 実施内容

- `scripts/export-tracked-files.ps1`
  - 冒頭の `param(...)` をやめ、コピー先パスをそのまま読める 1 行の文字列リテラルに変更した。
  - `C:\Users\shimadzu\OneDrive - SHIMADZU\共有\test` を `GetFullPath(...)` に渡す形にし、今後はこの 1 行を差し替えるだけで再利用できるようにした。
- `scripts/copy-git-files.bat`
  - `export-tracked-files.ps1` の `TargetRoot` を書き換えて使う想定であることをコメントで明示した。

## 検証

- `scripts/export-tracked-files.ps1`
  - PowerShell parser で構文エラーが出ないことを確認
- `scripts/export-tracked-files.ps1`
  - 先頭 10 数行を確認し、コピー先パスが文字列リテラル 1 行になっていることを確認
