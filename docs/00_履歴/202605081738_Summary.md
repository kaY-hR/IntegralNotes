# 2026-05-08 17:38 Inline Action 標準名の整理

## 依頼

- `@@mkpy` は分かりにくいため、`@@make-python-block` に変更する。
- `@@write` は「AI に指示する・お願いする」ニュアンスへ寄せ、`@@request` に変更する。
- promptless continuation は自動感が分かるように `@@auto-continue` に変更する。
- 既存 Python block 修正用として `@@amend-python-block` を追加する。
- 開発中でユーザーがいないため、旧 `continue` / `write` / `mkpy` は互換 alias として残さず廃止する。

## 対応

- fallback Inline Action 定義を新標準名へ変更した。
  - `auto-continue`
  - `request`
  - `make-python-block`
  - `amend-python-block`
  - `ask`
- `??` alias は `@@auto-continue`、`>>` alias は `@@make-python-block` に向けた。
- `Notes/.inline-action` の標準 action template を新名へ置換し、旧 `continue.md` / `write.md` / `mkpy.md` を削除した。
- 既存 workspace に旧 standard action file が残っていても、`continue` / `write` / `mkpy` は Inline Action として読み込まないようにした。
- Inline Action 設定 UI の説明文と、Python block 系 action の prompt placeholder を更新した。
- `docs/30_設計/40_InlineAction.md` を新名称・新標準 action 一覧へ更新した。

## 確認

- 履歴を除く `src` / `docs` / `Notes` から旧 user-facing 名 `@@continue` / `@@write` / `@@mkpy` / `mkpy` が残っていないことを確認した。
- `npm run build` 成功。
