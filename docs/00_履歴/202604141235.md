# 2026-04-14 12:35

## 会話要約

- Issue 13 を開始し、`links.json` ベースの source dataset 表現をやめて `.store` / `.integral` 前提の canonical storage へ寄せる実装を進めた。
- あわせて、関連する要求・アーキテクチャ・設計文書も現方針へ更新し、未実装 Issue への影響も確認した。

## 実施したこと

- `docs/10_要求`, `docs/20_アーキテクチャ`, `docs/30_設計` を `.store` / `.integral` / ID参照 / hard link / junction 前提へ更新した。
- `src/main/integralWorkspaceService.ts` を更新し、以下を新方針へ変更した。
  - original data の canonical 実体を `.store` へ置く
  - source dataset を `links.json` なしで materialize する
  - dataset 実行 path を `.store/{datasetId}` へ統一する
  - data note の path 表示を `Store Path` に変更する
- `src/main/workspaceService.ts` を更新し、以下を追加した。
  - legacy `.original-data` / `dataset` から `.store` への migration
  - source dataset legacy `links.json` から hard link / junction materialize への移行
  - `.store/.integral` ベースの data note 同期
- `src/renderer` の関連文言を新方針へ合わせて更新した。
- `docs/Issues.md` では Issue 13 を completed に更新した。

## 確認結果

- `npm run build` は成功した。
- workspace 内の一時ディレクトリで hard link / junction の作成・削除を試し、OS レベルの前提が満たせることを確認した。

## 補足

- `git status` は repository ownership の `safe.directory` 制約により実行できなかった。
