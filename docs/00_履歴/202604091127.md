# 2026-04-09 11:27 Summary

## 概要

- Milkdown 標準の code block UI / preview と独自 Integral UI が干渉していたため、独自 UI の対象を `json` 全般から `itg-notes` fenced block に限定した
- 非 `itg-notes` の code block は Milkdown 標準 `CodeMirrorBlock` に委譲する構成へ変更した

## 実施した対応

- `src\renderer\integralBlockRegistry.tsx`
  - Integral 専用 language として `itg-notes` 定数を追加
  - block 判定を `json` ではなく `itg-notes` のみに変更
- `src\renderer\integralCodeBlockFeature.tsx`
  - custom node view を `itg-notes` のときだけ使うよう変更
  - それ以外の code block は Milkdown 標準 node view へ委譲するよう変更
  - helper 文言を `itg-notes` 前提に更新
- `src\renderer\integralSnippetMenu.ts`
  - slash menu から挿入する fenced block を ````itg-notes` に変更
- `docs\10_要求\IntegralNotes.prj.md`
  - 要求ドキュメント内のサンプル fenced block を `itg-notes` に更新

## 検証

- `npm run build` 成功

## 補足

- 既存の `json` fenced block は今後 Integral UI には変換されず、通常の Milkdown code block として扱われる
- 既存ノートを UI 表示対象にしたい場合は、fence language を `itg-notes` に変更する必要がある
