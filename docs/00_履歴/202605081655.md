# 2026-05-08 16:55 Python block slot discovery 再発防止

## 依頼

- `>` から Python block を挿入したとき、`in` / `out` が YAML に入らない原因を受けて修正する。
- `Notes/.codex/skills/` を skill の truth として扱う。
- Python script 修正に加えて、skill / prompt を改善する。
- skill 内に Python script validator を置く案も反映する。

## 対応

- 外部 Obsidian workspace の `scripts/cell_detection/detect_cells.py` を修正した。
  - `inputs` / `outputs` の slot 定義を `dict(...)` から literal `{...}` mapping に変更。
  - output slot は `extensions=[".html"]` / `extensions=[".json"]` ではなく canonical な `extension` に変更。
- `Notes/.codex/skills/implement-integral-block` を更新した。
  - IntegralNotes discovery は静的に source を読むため、slot 定義は literal `{...}` にする必要があると明記。
  - `dict(...)`、変数、helper 関数、class instance による slot 定義を避けるよう追記。
  - single-file output は `extension` を優先するよう追記。
- skill-local validator を追加した。
  - `Notes/.codex/skills/implement-integral-block/scripts/validate_integral_block.py`
  - Python script を import / 実行せず、AST と source から `@integral_block` の discovery-compatible shape を検査する。
  - `dict(...)` slot、unsupported decorator key、slot key、params schema、signature などを検査する。
- app 内 fallback prompt / default prompt を更新した。
  - `src/main/aiChatService.ts`
  - `src/shared/aiChat.ts`
  - slot literal と output `extension` の注意を追加。

## 確認

- validator を対象 script に実行し、`OK: 1 block(s) checked, 0 warning(s).` を確認。
- `npm run build` 成功。

## 注意

- 直接原因は script 側の `dict(...)` slot 定義だった。
- 再発防止は skill / prompt / validator に寄せた。
- parser の `dict(...)` 対応は今回は実装していない。discovery 契約を literal `{...}` に固定する方針。
