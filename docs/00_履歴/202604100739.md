# 2026-04-10 07:39

## 背景

- `shimadzu-lc` の zip を更新する依頼があった。
- 既存の `plugins/shimadzu-lc/package.json` の `package:release` は、plugin ディレクトリ基準で `plugins/dist` を解決してしまい、自己コピーで失敗していた。

## 実施内容

- script 修正
  - `plugins/shimadzu-lc/package.json`
    - `package:release` に `--out-root ../../plugins/dist` を追加し、plugin ディレクトリから実行しても正しい出力先へ生成できるようにした。
  - `plugins/standard-graphs/package.json`
    - 同じ不具合を避けるため、同様に `--out-root ../../plugins/dist` を追加した。
  - `package.json`
    - root から個別に zip を生成できるよう
      - `plugins:package:shimadzu-lc`
      - `plugins:package:standard-graphs`
      を追加した。
- ドキュメント
  - `plugins/README.md`
  - `plugins/shimadzu-lc/README.md`
    - root からの個別 package script を追記した。
- 生成
  - `npm run plugins:package:shimadzu-lc`
    - `plugins/dist/shimadzu.lc/` を再生成した。

## 検証

- `plugins/dist/shimadzu.lc/shimadzu.lc-0.1.0.zip`
  - 更新時刻が `2026/04/10 07:39:05` になったことを確認
- zip 展開確認
  - `shimadzu.lc/gradient.png`
  - `shimadzu.lc/renderer/index.html`
  - `shimadzu.lc/host/index.cjs`
  - `shimadzu.lc/integral-plugin.json`
    が含まれることを確認
