# 2026-04-14 12:11

## 会話要約

- `links.json` 見直しの議論が進み、単なる擬似リンク形式の変更ではなく、`.store` を canonical storage にする構成へ寄せる方向が固まってきた。
- また、この変更は影響範囲が広いため、Issue では「文書更新を先に完了し、その後に実装し、最後に他 Issue の整合も見直す」という進め方を明記したい、という要望があった。

## 実施したこと

- Issue 13 の内容を、`links.json` の局所改善ではなく `.store` / `.integral` 前提の再設計 Issue として更新した。
- 併せて、進行順として
  - 文書の完全更新
  - 実装更新
  - 未実装 Issue の整合見直し
  を記録した。
