# 2026-04-14 17:12

## 会話要約

- `milkdown` が Obsidian 風の wiki link `[[aaa.md]]` を標準で扱えるか調査した。
- 併せて、IntegralNotes でその記法を導入したい要望を `docs/Issues.md` に追加した。

## 調査したこと

- `docs/10_要求` を確認し、note-first と data-note / note 本文からリンクで辿れる体験が要求に沿うことを再確認した。
- `src/renderer/MilkdownEditor.tsx` を確認し、現在は `Crepe` を生成して `featureConfigs` と custom block feature を入れているだけで、wiki link 専用拡張はまだ無いことを確認した。
- `node_modules/@milkdown/crepe/src/core/builder.ts` を確認し、Crepe の標準構成が `commonmark` と `gfm` ベースであることを確認した。
- Milkdown の公開 Discussion を確認し、custom syntax は `remark` plugin と schema / markdown transformer を追加して実装するのが基本方針であることを確認した。
- wiki link 用の remark plugin 実装例として `remark-wiki-link` 系の既存実装があることも確認した。

## 実施したこと

- `docs/Issues.md` に `Issue 22` を追加した。
  - title: `Milkdown で Obsidian 風 wiki link [[aaa.md]] を表示・編集したい`
  - 内容: `[[...]]` 記法対応、Milkdown custom syntax 拡張、app 内 link 解決規則、Issue 21 との記法整理

## 補足

- 結論として、Milkdown / Crepe が現状そのままで `[[aaa.md]]` を自然に扱うというより、`remark` ベースの独自拡張として入れる前提で考えるのが妥当と整理した。
- 今回は調査と Issue 追加のみで、コード実装や build / test は行っていない。
