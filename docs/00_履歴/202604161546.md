# 2026-04-16 15:46

- `.idts` の `![...](...idts)` 埋め込み表示について、一般的な viewer plugin の解決経路には乗せず、`idts` 専用特殊処理として既存の `DataSetViewer` 相当 (`DatasetRenderableView`) を使う方針で修正した。
- `src/renderer/workspaceEmbedFeature.tsx` で `.idts` 解決結果を `DatasetManifestFileViewer` ではなく `DatasetRenderableView` に差し替えた。これで embed 時の描画が既存の dataset 表示 block と同系統になった。
- 直前の実装で残していた `itg-notes` の JSON 互換を削除した。`src/renderer/integralBlockRegistry.tsx` は YAML のみを解釈・出力するように整理し、`src/renderer/integralCodeBlockFeature.tsx` も常に YAML として保存するように変更した。
- Python 用の新 block 挿入導線は `>` popup から YAML block を挿入する方式のまま維持し、旧 Python plugin 互換や JSON block source 互換は持たない構成に寄せた。
- `npm run build` を実行し、renderer/main ともにビルド成功を確認した。
