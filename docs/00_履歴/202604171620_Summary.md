# 2026-04-17 16:20 Summary

## 会話要約

- repo ローカルに `doc-review` スキルを作りたい、という依頼があった。
- スキルの目的は、`docs` 配下の md 文書同士、または仕様文書と実装の間にある矛盾点を探索すること。
- 仕様文書の対象は `docs` 配下の md で、`docs/00_履歴` は除外する、という条件が与えられた。

## 追加したもの

- `.codex/skills/doc-review/SKILL.md`
  - trigger 用 description に、`docs/00_履歴` を除外した `docs` 配下 md 文書と `src` 実装の整合確認に使うスキルであることを明記した。
  - 本文には、この repo 向けのレビュー手順を追加した。
    - `docs/10_要求` を起点に canonical な要求を拾う
    - `docs/20_アーキテクチャ` と `docs/30_設計` を同一論点で照合する
    - 必要な場合のみ `src` 実装まで追う
    - 指摘を `矛盾` / `未実装・追随不足` / `要確認` に分ける
    - findings-first で根拠付きに報告する
  - `integral-analysis` MCP は無関係なので使わない旨も明記した。

## 補足

- 既存の repo ローカル skill が簡素だったため、今回は `SKILL.md` のみで完結する最小構成にした。
