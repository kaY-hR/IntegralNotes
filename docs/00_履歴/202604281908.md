# 2026-04-28 会話まとめ

## 目的

?? の inline AI 挿入と >> の Python block 生成で、挿入元になった trigger marker が note に残るケースを防ぐ。

## 実装内容

- src/renderer/MilkdownEditor.tsx の AI 挿入処理に trigger cleanup を追加した。
- ?? / >> の popup 起動時削除に加えて、AI 結果を挿入する直前にも anchor 位置に残っている marker を検出して削除するようにした。
- marker の直後にも同じ文字列が実データとして存在する場合は、元からあった後続 text を誤削除しないように skip する条件を入れた。

## 変更ファイル

- src/renderer/MilkdownEditor.tsx

## 確認

- ユーザー指示がないため、ビルド・テストは未実行。
