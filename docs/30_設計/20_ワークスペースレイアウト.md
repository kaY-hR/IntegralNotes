# ワークスペースレイアウト

## 全体

```text
Workspace/
  <user-managed notes and folders>
  data-catalog/
  Data/
  .store/
  .py-scripts/
```

## ノート配置

- 通常ノートはユーザーが任意のフォルダに配置する
- 初回ワークスペース設定時に `Notes/` は自動作成しない
- `Notes/` という名前のフォルダを手動で使うこと自体は妨げないが、system 管理ディレクトリではない

## data-catalog

ユーザーがノートから参照する Markdown facade。

例:

```text
data-catalog/
  sample01.lcd.md
  samples.md
  DTS-9X4Q2M1A.md
```

方針:

- original data の data note は自動作成
- dataset の data note も source / derived を問わず自動作成
- original data の data note file 名は元ファイル名を使う
- dataset の data note file 名は dataset 名を使う
- file 名が重複する場合は `_<serial>` を付けて解消する
- metadata は frontmatter に保持する
- app 上では frontmatter を直接見せず、本文だけを編集・表示する
- metadata 同期時は本文を保持し、差分があるときだけ file を更新する

例:

```md
---
integralNoteType: "data-note"
dataTargetType: "original-data"
originalName: "sample01.lcd"
originalDataId: "ORD-7K2M9Q4D"
sourceKind: "file"
createdAt: "2026-04-14T04:17:00.000Z"
aliasRelativePath: "Data/sample01.lcd"
storeRelativePath: ".store/ORD-7K2M9Q4D.lcd"
---
# sample01.lcd
```

dataset data note 例:

```md
---
integralNoteType: "data-note"
dataTargetType: "dataset"
datasetId: "DTS-9X4Q2M1A"
name: "samples"
kind: "PYS-7K2M9Q4D.result"
createdAt: "2026-04-14T04:20:00.000Z"
createdByBlockId: "BLK-1F8E2D0A"
storeRelativePath: ".store/DTS-9X4Q2M1A"
---
# samples
```

## Data

`cwd` 外から能動登録した original data の visible alias を置く。

例:

```text
Data/
  sample01.lcd
  raw-run-001/
```

方針:

- file は hard link
- directory は junction
- 同名衝突時は `_{originalDataId}` を suffix として付ける
- `cwd` 内に最初からあった path を登録した場合は、`Data/` ではなく元の path 自体を alias とする

## .store

```text
.store/
  .integral/
    ORD-7K2M9Q4D.json
    DTS-9X4Q2M1A.json
  ORD-7K2M9Q4D.csv
  ORD-1A2B3C4D/
  DTS-9X4Q2M1A/
    result.html
    table.csv
```

### original data metadata 例

```json
{
  "aliasRelativePath": "Data/sample01.lcd",
  "originalDataId": "ORD-7K2M9Q4D",
  "originalName": "sample01.lcd",
  "createdAt": "2026-04-13T10:00:00+09:00",
  "sourceKind": "file",
  "storeRelativePath": ".store/ORD-7K2M9Q4D.lcd"
}
```

### original data 実体

```text
.store/
  ORD-7K2M9Q4D.csv
  ORD-1A2B3C4D/
    run-001/
      a.csv
      b.csv
```

### source dataset

```text
.store/
  .integral/
    DTS-1A2B3C4D.json
  DTS-1A2B3C4D/
    joined_analytical_data.csv
    Current.html
    run-001/
```

`DTS-1A2B3C4D.json` 例:

```json
{
  "datasetId": "DTS-1A2B3C4D",
  "name": "samples",
  "kind": "source-bundle",
  "createdByBlockId": null,
  "createdAt": "2026-04-13T10:05:00+09:00",
  "sourceMembers": [
    {
      "originalDataId": "ORD-7K2M9Q4D",
      "entryName": "joined_analytical_data.csv"
    },
    {
      "originalDataId": "ORD-9X4Q2M1A",
      "entryName": "Current.html"
    }
  ]
}
```

ルール:

- file original data は hard link で置く
- directory original data は junction で置く
- source dataset 直下には custom manifest を置かない
- name collision がある場合は `_{originalDataId}` を suffix として付けて解消する

### derived dataset

```text
.store/
  .integral/
    DTS-9X4Q2M1A.json
  DTS-9X4Q2M1A/
    result.html
    preview.png
```

`DTS-9X4Q2M1A.json` 例:

```json
{
  "datasetId": "DTS-9X4Q2M1A",
  "name": "DTS-9X4Q2M1A",
  "kind": "PYS-7K2M9Q4D.result",
  "createdByBlockId": "BLK-1F8E2D0A",
  "createdAt": "2026-04-13T10:10:00+09:00"
}
```

## .py-scripts

```text
.py-scripts/
  PYS-7K2M9Q4D/
    script.json
    pca.py
    helper.py
    config.json
    analysis-args.json
    stdout.log
    stderr.log
```

### ルール

- 実行場所は `PYS-...` フォルダ自身
- entry ファイル名は元のまま保持する
- 同梱ファイルはフラット配置する
- `analysis-args.json`, `stdout.log`, `stderr.log` は上書きされてよい

## GC 対象

MVP では、次を GC 対象にできる。

- どこからも参照されない dataset

一方、次は GC 対象にしない。

- original data
- `.py-scripts/PYS-...`
- data note

## 補足

- `.integral` を無視すれば `.store` は普通の filesystem として読める
- app からは `.store` 自体を非表示にしてよい

