# 20260417

<br />

`Data/LC_text_files` にデータファイルを配置した。

<br />

まずはこれらを右クリックして、データセット化した。

<br />

(ここで "\["を押して、"my\_LC\_Samples.idtsを選ぶとリンクが作れる")

[my\_LC\_Samples.idts](/datasets/my_LC_Samples.idts)

<br />

データセットをjsonに変換する。（ここで">"を押して、解析手段としてLC text to Chromatogram JSONを選ぶと、下記のブロックが挿入される）

<br />

```itg-notes
id: BLK-7FCB71AB
run: src/lc_text_to_chromatogram_json.py:main
in:
  source: /datasets/my_LC_Samples.idts
params: {}
out:
  json: /.store/.integral/datasets/DTS-D3DFD014.idts
```

<br />

`//そもそもユーザーが中間ファイル(json)を意識するのはイケてなさすぎる`

<br />

正しく変換できているか確認のため、可視化。（ここで">"を打って、..to htmlを選ぶと、下記のブロックが挿入される）

`//読み込んだ時点で可視化すべき。PCAとか数値処理のためにjsonは必要だが、jsonと一緒に可視化プロットまで出しても良い。`

```itg-notes
id: BLK-EE1E8333
run: src/chromatogram_json_to_plotly_html.py:main
in:
  source: /.store/.integral/datasets/DTS-D3DFD014.idts
params: {}
out:
  plot: /.store/.integral/datasets/DTS-4FDF8D60.idts
```

<br />

（ここで、何故かソリューションパネル上部の目のアイコンをクリックして、隠しフォルダを表示しないといけない）`要修正！！`

(ここで、上部にDTS-4F...とあるので、"!\[" と打ってDTS-4FDF..を選ぶと、下記のブロックが挿入される)

`//いちいち、結果のDTS-....を打たないと結果が見れないし、解析のinputにもできない。これは絶対NG。修正する`

![](/.store/.integral/datasets/DTS-4FDF8D60.idts)

４本のピークがあるクロマトグラムである。

<br />

次に、PCAを実行

```itg-notes
id: BLK-07ED0143
run: src/chromatogram_pca.py:main
in:
  source: /.store/.integral/datasets/DTS-D3DFD014.idts
params: {}
out:
  report: /.store/.integral/datasets/DTS-21662FA2.idts
```

<br />

PCAの結果を描画。

`//一発で可視化までしたいところ。`

![](/.store/.integral/datasets/DTS-21662FA2.idts)
